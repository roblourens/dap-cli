---
phase: 06-add-conditional-breakpoint-playwright-interop-coverage
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/controller/childSessions.ts
  - tests/controller/sessionManager.test.ts
autonomous: true
requirements:
  - DBG-01
  - TEST-05
  - TEST-07
must_haves:
  truths:
    - "D-05: conditional/logpoint metadata survives the existing DAP-first controller routing path without a new capability gate."
    - "A fake multi-process parent fans out `condition`, `hitCondition`, and `logMessage` unchanged to every child."
    - "A js-debug parent route preserves the same metadata when it updates the parent provisional breakpoint registry and merges child verification results."
  artifacts:
    - path: "tests/controller/sessionManager.test.ts"
      provides: "Regression tests for conditional breakpoint metadata through child-session routing."
      contains: "hitCondition"
    - path: "src/controller/childSessions.ts"
      provides: "Production routing fix only if tests reveal metadata is dropped."
      contains: "setBreakpoints"
  key_links:
    - from: "tests/controller/sessionManager.test.ts"
      to: "ChildSessionCoordinator.maybeIntercept('setBreakpoints', args)"
      via: "assert captured parent/child DAP arguments retain metadata"
      pattern: "maybeIntercept\('setBreakpoints'"
---

<objective>
Pin conditional breakpoint metadata through child-session routing.

Purpose: The alias can construct the right payload, but Phase 6 is not complete unless the payload reaches real routed sessions used by js-debug browser workflows.
Output: Controller routing regressions that prove metadata preservation for fake multi-process and js-debug parent flows, plus a production fix if needed.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-add-conditional-breakpoint-playwright-interop-coverage/06-CONTEXT.md
@.planning/phases/06-add-conditional-breakpoint-playwright-interop-coverage/06-RESEARCH.md
@.planning/phases/06-add-conditional-breakpoint-playwright-interop-coverage/06-01-SUMMARY.md
@src/controller/childSessions.ts
@tests/controller/sessionManager.test.ts

<interfaces>
Existing controller tests already instantiate `ChildSessionCoordinator` with fake endpoints and call:
```typescript
const result = (await coordinator.maybeIntercept('setBreakpoints', {
  source: { path: 'app.js' },
  breakpoints: [{ line: 2 }],
}))?.value;
```

Existing invariants to preserve:
- fake multi-process: all children receive `setBreakpoints`; any verified child makes the aggregated breakpoint verified.
- js-debug: parent receives exactly one `setBreakpoints`; page child verification can override provisional parent `verified:false`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add routing regression tests for conditional breakpoint metadata</name>
  <files>tests/controller/sessionManager.test.ts</files>
  <read_first>tests/controller/sessionManager.test.ts, src/controller/childSessions.ts, .planning/phases/06-add-conditional-breakpoint-playwright-interop-coverage/06-01-SUMMARY.md</read_first>
  <behavior>
    - Fake multi-process test: call `coordinator.maybeIntercept('setBreakpoints', { source: { path: 'foo.js' }, breakpoints: [{ line: 10, condition: 'left > 3', hitCondition: '2', logMessage: 'left={left}' }] })` and assert every child endpoint captured the same metadata in `req.arguments.breakpoints[0]`.
    - js-debug parent route test: configure parent and child endpoints like the existing verified merge test, call `maybeIntercept` with the same metadata, and assert the parent received one request containing all three metadata fields and the page child received one request containing all three metadata fields.
    - Tests must not assert adapter capability flags because D-05 keeps this DAP-first.
  </behavior>
  <action>Add two tests near the existing `setBreakpoints` child-session tests. Reuse `FakeAdapterEndpoint`, `SessionManager.create`, `DapClient`, and `ChildSessionCoordinator` setup already present in the file. Use precise local response/captured-arguments types; do not use `any` or unsafe casts.</action>
  <acceptance_criteria>
    - `tests/controller/sessionManager.test.ts` contains a test name mentioning `conditional metadata`.
    - Assertions check `condition`, `hitCondition`, and `logMessage` on captured DAP request arguments.
    - Existing verified merge assertions for js-debug remain intact.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/controller/sessionManager.test.ts</automated>
  </verify>
  <done>Controller tests fail if child-session routing drops or rewrites conditional breakpoint metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix routing only if metadata preservation fails</name>
  <files>src/controller/childSessions.ts, tests/controller/sessionManager.test.ts</files>
  <read_first>src/controller/childSessions.ts, tests/controller/sessionManager.test.ts</read_first>
  <behavior>
    - If Task 1 tests pass without production edits, leave `src/controller/childSessions.ts` unchanged and record that in the summary.
    - If tests fail because routing reconstructs breakpoint objects and omits metadata, update only the `setBreakpoints` routing path to forward the original breakpoint objects while preserving existing verified/warnings merge behavior.
    - Do not add capability pre-checking or adapter-specific semantic evaluation.
    - Do not change thread/frame/variable routing in this plan.
  </behavior>
  <action>Run Task 1 tests. If they fail from metadata loss, patch `ChildSessionCoordinator` so routed `setBreakpoints` requests pass `condition`, `hitCondition`, and `logMessage` through unchanged to parent and child requests. Keep current aggregation rules: any verified child can verify the aggregate, provisional parent messages are dropped on verified child merge, and warnings remain visible.</action>
  <acceptance_criteria>
    - `npm test -- tests/controller/sessionManager.test.ts` exits 0.
    - If `src/controller/childSessions.ts` changes, grep shows no new `supportsConditionalBreakpoints` or `supportsLogPoints` capability branch.
    - Captured parent and child arguments in tests include the three metadata fields exactly as input strings.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm test -- tests/controller/sessionManager.test.ts</automated>
  </verify>
  <done>Conditional breakpoint metadata is pinned through production routing or proven already preserved by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Parent session -> child DAP sessions | User breakpoint metadata crosses from parent CLI request into child adapter connections. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-06-02-01 | Tampering | `ChildSessionCoordinator` setBreakpoints routing | mitigate | Tests assert exact metadata preservation; routing must forward user-authored DAP fields instead of reconstructing a lossy object. |
| T-06-02-02 | Spoofing | Aggregated breakpoint verification | mitigate | Preserve existing verified/warnings aggregation tests so metadata work does not make unverified breakpoints appear verified. |
| T-06-02-03 | Denial of service | Child fan-out | accept | This plan does not increase fan-out cardinality; it adds fields to existing requests only. |
</threat_model>

<verification>
Run `npm run typecheck` and `npm test -- tests/controller/sessionManager.test.ts`.
</verification>

<success_criteria>
Metadata emitted by Plan 01 reaches child-session routing unchanged, and no adapter capability gate or routing redesign is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/06-add-conditional-breakpoint-playwright-interop-coverage/06-02-SUMMARY.md`.
</output>
