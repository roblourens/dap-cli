---
phase: 06-add-conditional-breakpoint-playwright-interop-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/commands/dapAliases.ts
  - tests/fixtures/fake-adapter-entry.ts
  - tests/integration/fakeAdapterCli.test.ts
autonomous: true
requirements:
  - DBG-01
  - TEST-03
  - TEST-05
must_haves:
  truths:
    - "D-01/D-02/D-03: `breakpoints set` exposes `--condition`, `--hit-condition`, and `--log-message`."
    - "D-04: when multiple `--line` values are supplied, each requested breakpoint receives the same condition, hit condition, and log message metadata."
    - "D-05: the alias sends DAP breakpoint metadata directly and lets adapter responses report `verified`, `message`, and warnings."
  artifacts:
    - path: "src/cli/commands/dapAliases.ts"
      provides: "Friendly breakpoint alias flags and DAP setBreakpoints payload construction."
      contains: "hitCondition"
    - path: "tests/fixtures/fake-adapter-entry.ts"
      provides: "Fake adapter script that rejects malformed conditional breakpoint payloads."
      contains: "expect-conditional-breakpoints"
    - path: "tests/integration/fakeAdapterCli.test.ts"
      provides: "Always-runnable CLI integration coverage for the alias flag shape."
      contains: "--hit-condition"
  key_links:
    - from: "src/cli/commands/dapAliases.ts"
      to: "setBreakpoints DAP request"
      via: "breakpoints set alias maps every line to a breakpoint object with metadata"
      pattern: "breakpoints: lines.map"
---

<objective>
Expose conditional breakpoint metadata through the friendly breakpoint alias.

Purpose: Agents should not have to drop to raw `setBreakpoints --json` just to use common DAP conditional breakpoint fields.
Output: CLI flags, DAP-shaped payload mapping, and fake-adapter integration tests that fail if the payload is wrong.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-add-conditional-breakpoint-playwright-interop-coverage/06-CONTEXT.md
@.planning/phases/06-add-conditional-breakpoint-playwright-interop-coverage/06-RESEARCH.md
@src/cli/commands/dapAliases.ts
@tests/fixtures/fake-adapter-entry.ts
@tests/integration/fakeAdapterCli.test.ts

<interfaces>
From `src/cli/commands/dapAliases.ts`:
```typescript
interface BreakpointsSetOptions extends NamedOptions {
  source: string;
  line?: string[];
}

breakpoints.command('set')
  .requiredOption('--source <path>', 'source path')
  .requiredOption('--line <number...>', 'breakpoint line')
  .option('--name <name>', 'session name or id')
  .action(async (options: BreakpointsSetOptions) => {
    const lines = parseIntegerValues(options.line, 'line');
    await sendAliasRequest(output, 'setBreakpoints', {
      source: { path: options.source },
      breakpoints: lines.map(line => ({ line })),
      lines,
    }, options.name, 'breakpoints set');
  });
```

From `tests/fixtures/fake-adapter-entry.ts`:
```typescript
function matchesExpectedArguments(actual: unknown, expected: Record<string, unknown>): boolean;
function createLifecycleScript(startCommand: 'launch' | 'attach', expectedStartArguments?: Record<string, unknown>): FakeStep[];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing fake-adapter coverage for conditional breakpoint alias payloads</name>
  <files>tests/fixtures/fake-adapter-entry.ts, tests/integration/fakeAdapterCli.test.ts</files>
  <read_first>tests/fixtures/fake-adapter-entry.ts, tests/integration/fakeAdapterCli.test.ts, src/cli/commands/dapAliases.ts</read_first>
  <behavior>
    - Test launches the fake adapter script `expect-conditional-breakpoints` under session name `conditional-alias`.
    - Test invokes `breakpoints set --name conditional-alias --source app.js --line 5 9 --condition "left > 3" --hit-condition 2 --log-message "left={left}"`.
    - Fake adapter receives exactly `setBreakpoints` arguments: `source: { path: "app.js" }`, `lines: [5, 9]`, and `breakpoints: [{ line: 5, condition: "left > 3", hitCondition: "2", logMessage: "left={left}" }, { line: 9, condition: "left > 3", hitCondition: "2", logMessage: "left={left}" }]`.
    - The test must fail before Task 2 because the current alias does not know these options.
  </behavior>
  <action>Add a fake adapter script named `expect-conditional-breakpoints`. Build it from `createLifecycleScript('launch').slice(0, 5)`, then a `setBreakpoints` step with the exact `expectedArguments` above, then `disconnect`, `terminated`, and `close` steps. Add the integration test to `tests/integration/fakeAdapterCli.test.ts` near existing fake adapter CLI command coverage. Use `runCli`; do not call controller internals directly.</action>
  <acceptance_criteria>
    - `tests/fixtures/fake-adapter-entry.ts` contains `expect-conditional-breakpoints`.
    - `tests/integration/fakeAdapterCli.test.ts` contains `--condition`, `--hit-condition`, and `--log-message` in one `runCli` call.
    - The test asserts the `breakpoints set` command exits 0 after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/integration/fakeAdapterCli.test.ts</automated>
  </verify>
  <done>Coverage exists for multi-line metadata replication and fails if any DAP field is missing or renamed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement conditional breakpoint flags on breakpoints set</name>
  <files>src/cli/commands/dapAliases.ts</files>
  <read_first>src/cli/commands/dapAliases.ts, tests/integration/fakeAdapterCli.test.ts</read_first>
  <behavior>
    - `BreakpointsSetOptions` includes `condition?: string`, `hitCondition?: string`, and `logMessage?: string`.
    - The command has `.option('--condition <expr>', 'breakpoint condition')`, `.option('--hit-condition <expr>', 'breakpoint hit condition')`, and `.option('--log-message <text>', 'breakpoint log message')`.
    - For every parsed line, the breakpoint object includes `line` plus only the metadata fields whose option values are not `undefined`.
    - The top-level `lines` array remains present because existing DAP alias behavior already sends it.
    - No adapter capability pre-check is added, per D-05.
  </behavior>
  <action>Update `BreakpointsSetOptions`, register the three options, and build `breakpoints` with `compactObject({ line, condition: options.condition, hitCondition: options.hitCondition, logMessage: options.logMessage })`. Keep `parseIntegerValues(options.line, 'line')` unchanged and keep `sendAliasRequest(output, 'setBreakpoints', ...)` as the only request path.</action>
  <acceptance_criteria>
    - `src/cli/commands/dapAliases.ts` contains `.option('--hit-condition <expr>'`.
    - `src/cli/commands/dapAliases.ts` contains `hitCondition: options.hitCondition`.
    - `src/cli/commands/dapAliases.ts` does not contain a new adapter capability branch for breakpoint metadata.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm test -- tests/integration/fakeAdapterCli.test.ts</automated>
  </verify>
  <done>`breakpoints set` accepts the three metadata flags and the fake adapter proves the exact DAP request payload.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| CLI user input -> DAP adapter request | User-supplied condition/log expressions become adapter-evaluated breakpoint metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-06-01-01 | Tampering | `breakpoints set` payload construction | mitigate | Preserve fields as plain strings in DAP-shaped request objects; do not evaluate or transform expressions inside dap-cli. |
| T-06-01-02 | Information disclosure | `--log-message` examples/tests | accept | Log messages are user-authored debug metadata; docs/tests use fixture-only values and no secrets. |
| T-06-01-03 | Denial of service | multi-line breakpoint expansion | accept | Existing `--line <number...>` behavior already expands one object per line; metadata copying does not add loops beyond that existing list. |
</threat_model>

<verification>
Run `npm run typecheck` and `npm test -- tests/integration/fakeAdapterCli.test.ts`.
</verification>

<success_criteria>
The user-facing alias exposes `condition`, `hitCondition`, and `logMessage`; multi-line requests copy metadata to every breakpoint; no capability gate is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/06-add-conditional-breakpoint-playwright-interop-coverage/06-01-SUMMARY.md`.
</output>
