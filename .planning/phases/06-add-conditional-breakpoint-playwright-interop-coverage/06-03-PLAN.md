---
phase: 06-add-conditional-breakpoint-playwright-interop-coverage
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - tests/integration/playwrightInterop.test.ts
  - docs/PLAYWRIGHT-INTEROP.md
  - .planning/phases/06-add-conditional-breakpoint-playwright-interop-coverage/06-UAT.md
autonomous: true
requirements:
  - DBG-01
  - AGNT-04
  - AGNT-05
  - TEST-04
  - TEST-07
must_haves:
  truths:
    - "Playwright-triggered browser code does not pause when the conditional breakpoint expression is false."
    - "The same Playwright-controlled browser pauses with `reason: breakpoint` when the conditional breakpoint expression is true."
    - "Docs show agents how to set conditional, hit-condition, and logpoint metadata while preserving the polling-only workflow."
    - "Phase UAT records exact automated command lines and the gated smoke result or prerequisite block."
  artifacts:
    - path: "tests/integration/playwrightInterop.test.ts"
      provides: "Gated same-browser conditional breakpoint smoke using js-debug and Playwright."
      contains: "--condition"
    - path: "docs/PLAYWRIGHT-INTEROP.md"
      provides: "Agent-facing conditional breakpoint workflow."
      contains: "--hit-condition"
    - path: ".planning/phases/06-add-conditional-breakpoint-playwright-interop-coverage/06-UAT.md"
      provides: "Execution transcript for Phase 6 automated and gated verification."
      contains: "Conditional Breakpoint Playwright Interop"
  key_links:
    - from: "tests/integration/playwrightInterop.test.ts"
      to: "tests/fixtures/simple-chrome-page/app.js"
      via: "Playwright evaluates calculate(...) while dap-cli polls stopped events"
      pattern: "calculate\(7, 8\)"
    - from: "docs/PLAYWRIGHT-INTEROP.md"
      to: "breakpoints set alias"
      via: "documented flags match implemented CLI flags"
      pattern: "--condition"
---

<objective>
Prove and document conditional breakpoint interop with Playwright.

Purpose: Phase 6 is about agent workflow confidence, not just CLI parsing; a browser action driven by Playwright must demonstrate the conditional breakpoint semantics agents will rely on.
Output: Gated real js-debug/Playwright smoke coverage, docs, and UAT transcript.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-add-conditional-breakpoint-playwright-interop-coverage/06-CONTEXT.md
@.planning/phases/06-add-conditional-breakpoint-playwright-interop-coverage/06-RESEARCH.md
@.planning/phases/06-add-conditional-breakpoint-playwright-interop-coverage/06-01-SUMMARY.md
@.planning/phases/06-add-conditional-breakpoint-playwright-interop-coverage/06-02-SUMMARY.md
@tests/integration/playwrightInterop.test.ts
@tests/fixtures/simple-chrome-page/app.js
@docs/PLAYWRIGHT-INTEROP.md

<interfaces>
Existing gated test prerequisites from `tests/integration/playwrightInterop.test.ts`:
```typescript
const hasJsDebug = existsSync(jsDebugPath) || existsSync(localJsDebugPath);
const runChromePlaywrightHandoff = process.env.DAP_CLI_RUN_CHROME_PLAYWRIGHT_HANDOFF === '1';
test.skipIf(!runChromePlaywrightHandoff || !hasJsDebug)('coordinates Playwright with the same Chromium target attached by js-debug', async (ctx) => { ... });
```

Existing helper to adapt:
```typescript
async function waitForStoppedEvent(name: string): Promise<{ event: string; body?: unknown } | undefined>;
```
For this plan, update or add a helper that can poll with a caller-provided timeout and, if needed, an `after-cursor` so the false-condition assertion only observes events after the conditional breakpoint is set.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add gated Playwright/js-debug conditional breakpoint smoke</name>
  <files>tests/integration/playwrightInterop.test.ts</files>
  <read_first>tests/integration/playwrightInterop.test.ts, tests/fixtures/simple-chrome-page/app.js, .planning/phases/06-add-conditional-breakpoint-playwright-interop-coverage/06-01-SUMMARY.md, .planning/phases/06-add-conditional-breakpoint-playwright-interop-coverage/06-02-SUMMARY.md</read_first>
  <behavior>
    - New gated test in the existing `Playwright interop` describe block, using `test.skipIf(!runChromePlaywrightHandoff || !hasJsDebug)`.
    - The test provisions js-debug, launches a persistent Chromium context with a fixed free CDP port, attaches dap-cli as `chrome-conditional-breakpoint`, waits until `threads --name chrome-conditional-breakpoint` returns at least one thread, and sets a breakpoint on `tests/fixtures/simple-chrome-page/app.js` line 2 through `breakpoints set --condition "left === 7 && right === 8"`.
    - False path: run `realPage.evaluate('setTimeout("calculate(1, 2)", 0)')`, assert the page result becomes `3`, and assert no new stopped event appears for this session within 1000 ms.
    - True path: run `realPage.evaluate('setTimeout("calculate(7, 8)", 0)')`, poll stopped events for up to 10000 ms, assert `reason` is `breakpoint`, then inspect `threads`, `stack`, `scopes`, and `variables` through the parent name and assert locals include `left` value `7` and `right` value `8`.
    - Continue the paused thread and assert the page result becomes `15`, then close the dap-cli session and browser context.
  </behavior>
  <action>Add the gated test using the existing same-browser handoff setup as the template. Extend helper code only as needed: either add `waitForStoppedEvent(name, timeoutMs, afterCursor?)` or add a second helper that polls `events --name <name> --include stopped --after-cursor <cursor> --limit 50`. Use existing zod schemas where possible and extend `breakpointsSetDataSchema` only if the test needs to read extra fields. Keep cleanup in `finally` with `runCli(['stop' or 'close', '--name', 'chrome-conditional-breakpoint'])` and `realContext?.close()`.</action>
  <acceptance_criteria>
    - `tests/integration/playwrightInterop.test.ts` contains `chrome-conditional-breakpoint`.
    - The test command uses `breakpoints set` with `--condition`.
    - The false path checks absence of a stopped event after `calculate(1, 2)`.
    - The true path checks a stopped event and local variables for `left` and `right`.
  </acceptance_criteria>
  <verify>
    <automated>DAP_CLI_RUN_CHROME_PLAYWRIGHT_HANDOFF=1 npm test -- tests/integration/playwrightInterop.test.ts</automated>
  </verify>
  <done>The gated interop test proves false and true conditional breakpoint behavior in the same Playwright-controlled browser session.</done>
</task>

<task type="auto">
  <name>Task 2: Update Playwright interop docs and record Phase 6 UAT</name>
  <files>docs/PLAYWRIGHT-INTEROP.md, .planning/phases/06-add-conditional-breakpoint-playwright-interop-coverage/06-UAT.md</files>
  <read_first>docs/PLAYWRIGHT-INTEROP.md, .planning/phases/06-add-conditional-breakpoint-playwright-interop-coverage/06-CONTEXT.md, .planning/phases/06-add-conditional-breakpoint-playwright-interop-coverage/06-VALIDATION.md</read_first>
  <action>Update `docs/PLAYWRIGHT-INTEROP.md` to include a conditional breakpoint example directly after the existing `breakpoints set` example. The docs must show `--condition "count === 1"`, `--hit-condition 2`, and `--log-message "handleClick count={count}"` as DAP metadata examples, state that dap-cli passes these fields through to adapters, and keep the polling sequence (`events`, `threads`, `stack`, `scopes`, `variables`, `continue`) unchanged. Create `06-UAT.md` with headings `## Automated Verification`, `## Gated Conditional Breakpoint Smoke`, and `## Repo Hard-Rule Verify-Work Reminder`. Record exact command lines and exit codes for `npm run typecheck`, `npm test -- tests/integration/fakeAdapterCli.test.ts tests/controller/sessionManager.test.ts`, `DAP_CLI_RUN_CHROME_PLAYWRIGHT_HANDOFF=1 npm test -- tests/integration/playwrightInterop.test.ts`, and `npm test -- tests/integration/docsValidation.test.ts`. If the gated smoke is skipped or blocked by missing js-debug/Chrome setup, record `result: blocked` with the exact skip/prerequisite message; do not mark it pass.</action>
  <acceptance_criteria>
    - `docs/PLAYWRIGHT-INTEROP.md` contains `--condition`, `--hit-condition`, and `--log-message`.
    - `06-UAT.md` contains `Gated Conditional Breakpoint Smoke` and either `result: pass` or `result: blocked`.
    - `06-UAT.md` reminds verify-work to hand-drive `docs/HAND-DRIVEN-SMOKE.md` Sequence A and Sequence B per repo rule.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/integration/docsValidation.test.ts &amp;&amp; grep -v '^#' .planning/phases/06-add-conditional-breakpoint-playwright-interop-coverage/06-UAT.md | grep -Eq 'result: (pass|blocked)'</automated>
  </verify>
  <done>Docs teach the conditional breakpoint workflow and UAT records the real verification state honestly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Playwright -> browser page | Test code drives a live Chromium page while dap-cli controls debugger state. |
| Browser page -> dap-cli inspection output | Local variables and source paths are recorded in test output and UAT. |
| Docs/UAT -> repo | Local command transcripts become checked-in planning/docs artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-06-03-01 | Denial of service | gated Playwright/js-debug smoke | mitigate | Use existing `finally` cleanup for dap-cli session and Chromium context; continue paused thread before teardown. |
| T-06-03-02 | Information disclosure | `06-UAT.md` transcript | mitigate | Record fixture values, command lines, and test summaries only; do not include secrets or auth material. |
| T-06-03-03 | Repudiation | blocked gated smoke | mitigate | UAT must record `result: blocked` with exact prerequisite text if js-debug/Chrome is unavailable; no silent pass. |
</threat_model>

<verification>
Run `npm run typecheck`, `npm test -- tests/integration/fakeAdapterCli.test.ts tests/controller/sessionManager.test.ts`, `DAP_CLI_RUN_CHROME_PLAYWRIGHT_HANDOFF=1 npm test -- tests/integration/playwrightInterop.test.ts`, and `npm test -- tests/integration/docsValidation.test.ts`.
</verification>

<success_criteria>
The gated smoke proves conditional pause semantics in a Playwright-controlled browser; docs expose the workflow; UAT records the verification result or exact prerequisite block.
</success_criteria>

<output>
After completion, create `.planning/phases/06-add-conditional-breakpoint-playwright-interop-coverage/06-03-SUMMARY.md`.
</output>
