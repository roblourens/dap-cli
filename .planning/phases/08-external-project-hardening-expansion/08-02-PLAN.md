---
phase: 08-external-project-hardening-expansion
plan: 2
type: fix
wave: 1
depends_on:
  - 08-01-PLAN.md
files_modified:
  - src/config/launchConfig.ts
  - src/controller/childSessions.ts
  - src/controller/diagnostics.ts
  - tests/config/launchConfig.test.ts
  - tests/controller/childSessions.test.ts
  - tests/controller/sessionManager.test.ts
  - .planning/phases/08-external-project-hardening-expansion/08-UAT.md
  - .planning/phases/08-external-project-hardening-expansion/08-01-SUMMARY.md
  - .planning/phases/08-external-project-hardening-expansion/08-02-SUMMARY.md
autonomous: true
requirements:
  - AGNT-02
  - AGNT-03
  - TEST-05
  - TEST-07
tags: [hardening, gap-closure, launch-json, diagnostics]
must_haves:
  truths:
    - "Modern VS Code Python launch configs using type: debugpy resolve to the built-in debugpy adapter."
    - "Paused-only child-session inspection failures surface thread_not_paused guidance instead of controller_unavailable recovery text."
    - "JS pwa-node breakpoint verification timeouts include actionable source-path and timing diagnostics."
  artifacts:
    - path: "tests/config/launchConfig.test.ts"
      provides: "Regression coverage for debugpy launch type mapping"
      contains: "resolveAdapterIdFromType('debugpy')"
    - path: "tests/controller/childSessions.test.ts"
      provides: "Regression coverage for child not-paused stackTrace normalization"
      contains: "thread_not_paused"
    - path: "tests/controller/sessionManager.test.ts"
      provides: "Regression coverage for JS breakpoint timeout diagnostics"
      contains: "For JavaScript source breakpoints"
  key_links:
    - from: ".planning/phases/08-external-project-hardening-expansion/08-UAT.md"
      to: "src/config/launchConfig.ts"
      via: "GAP-08-01 closure"
      pattern: "debugpy: 'debugpy'"
    - from: ".planning/phases/08-external-project-hardening-expansion/08-UAT.md"
      to: "src/controller/childSessions.ts"
      via: "GAP-08-02 closure"
      pattern: "threadNotPaused"
    - from: ".planning/phases/08-external-project-hardening-expansion/08-UAT.md"
      to: "src/controller/diagnostics.ts"
      via: "GAP-08-03 closure"
      pattern: "For JavaScript source breakpoints"
---

<objective>
Close the product gaps discovered by the Phase 8 external repository hardening expansion.

Purpose: Convert the external-project failures into focused compatibility and diagnostics fixes while preserving the broader Phase 8 evidence as the source of truth.

Output: Code fixes, focused regression tests, and updated Phase 8 UAT/status artifacts showing GAP-08-01 through GAP-08-03 closed.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-external-project-hardening-expansion/08-UAT.md
@.planning/phases/08-external-project-hardening-expansion/08-01-SUMMARY.md
@src/config/launchConfig.ts
@src/controller/childSessions.ts
@src/controller/diagnostics.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map modern debugpy launch configs</name>
  <files>src/config/launchConfig.ts, tests/config/launchConfig.test.ts</files>
  <read_first>
    - src/config/launchConfig.ts
    - tests/config/launchConfig.test.ts
  </read_first>
  <action>
Add `debugpy` as a first-class VS Code launch type mapping to the built-in debugpy adapter, matching modern Python launch.json files observed in Phase 8.
  </action>
  <acceptance_criteria>
    - `resolveAdapterIdFromType('debugpy')` returns `debugpy`.
    - Existing `python` mapping remains supported.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/config/launchConfig.test.ts</automated>
  </verify>
  <done>GAP-08-01 has code and regression coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize child running-thread inspection errors</name>
  <files>src/controller/childSessions.ts, tests/controller/childSessions.test.ts</files>
  <read_first>
    - src/controller/childSessions.ts
    - src/controller/diagnostics.ts
    - tests/controller/childSessions.test.ts
  </read_first>
  <action>
Catch child-session paused-only DAP failures for stack/scopes/variables routing and convert adapter "not paused" responses into the shared `thread_not_paused` diagnostic contract.
  </action>
  <acceptance_criteria>
    - Child-routed `stackTrace` not-paused failures no longer surface controller recovery guidance.
    - Regression test asserts `thread_not_paused` and the events/stop-on-entry diagnostic.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/controller/childSessions.test.ts</automated>
  </verify>
  <done>GAP-08-02 has code and regression coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Improve JS breakpoint binding guidance</name>
  <files>src/controller/diagnostics.ts, tests/controller/sessionManager.test.ts</files>
  <read_first>
    - src/controller/diagnostics.ts
    - tests/controller/sessionManager.test.ts
  </read_first>
  <action>
Extend js-debug breakpoint verification timeout diagnostics for JavaScript sources so users get actionable source-path comparison and timing guidance when a real pwa-node breakpoint remains unbound.
  </action>
  <acceptance_criteria>
    - JavaScript timeout warnings mention comparing requested source paths with stopped stack frame paths.
    - JavaScript timeout warnings mention `stopOnEntry` or setting breakpoints after child registration for quick-exit/lazy-loaded programs.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/controller/sessionManager.test.ts</automated>
  </verify>
  <done>GAP-08-03 has diagnostics and regression coverage.</done>
</task>

<task type="auto">
  <name>Task 4: Record Phase 8 gap closure</name>
  <files>.planning/phases/08-external-project-hardening-expansion/08-UAT.md, .planning/phases/08-external-project-hardening-expansion/08-01-SUMMARY.md, .planning/phases/08-external-project-hardening-expansion/08-02-SUMMARY.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/phases/08-external-project-hardening-expansion/08-UAT.md
    - .planning/ROADMAP.md
  </read_first>
  <action>
Update Phase 8 UAT, summaries, and GSD state to show the filed gaps are closed and the phase is complete.
  </action>
  <acceptance_criteria>
    - GAP-08-01 through GAP-08-03 are marked closed with fix and verification notes.
    - Phase 8 status docs no longer say gap planning is pending.
    - GSD validators and `git diff --check` pass.
  </acceptance_criteria>
  <verify>
    <automated>node .github/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/08-external-project-hardening-expansion/08-02-PLAN.md</automated>
    <automated>node .github/get-shit-done/bin/gsd-tools.cjs validate consistency</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>Phase 8 is ready to commit as gap-closed.</done>
</task>

</tasks>
