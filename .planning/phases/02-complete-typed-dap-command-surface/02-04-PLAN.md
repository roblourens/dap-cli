---
id: 02-04
phase: 02-complete-typed-dap-command-surface
title: Add generated command coverage tests and deterministic scripted feature coverage
wave: 4
depends_on:
  - 02-01
  - 02-02
  - 02-03
requirements_addressed:
  - DAP-03
  - DAP-04
  - DAP-05
  - DBG-01
  - DBG-02
  - DBG-03
  - DBG-04
  - TEST-02
  - TEST-05
files_modified:
  - src/testing/dapScript.ts
  - src/testing/fakeAdapter.ts
  - tests/fixtures/fake-adapter-entry.ts
  - tests/cli/dapGeneratedCommands.test.ts
  - tests/integration/fakeAdapterCli.test.ts
  - tests/protocol/fakeAdapter.test.ts
  - tests/architecture/moduleBoundaries.test.ts
autonomous: true
must_haves:
  truths:
    - "D-01: Coverage tests derive expected request inventory from official DAP protocol metadata, not from a handwritten list."
    - "D-02: Generated registry diffs are deterministic and reviewable."
    - "D-03: Ergonomic aliases and generated commands are tested to converge on generated metadata and `dap.request`."
    - "D-04: Raw request passthrough remains covered as an escape hatch."
    - "D-05: Breakpoint, inspection, evaluation, and execution-control aliases are covered with deterministic fake-adapter scripts."
    - "D-06: JSON envelope, handled errors, session targeting, and controller IPC contracts stay covered."
    - "D-07: Unsupported capability failures are tested as structured handled JSON."
    - "D-08: Capability reporting is tested before or after command execution with request/session/adapter context on failures."
    - "D-09: Generated command inventory tests fail when official DAP requests are missing from the registry."
    - "D-10: Fake-adapter tests cover representative success, unsupported capability, paused/unpaused, raw passthrough, and ergonomic alias behavior."
  artifacts:
    - path: "tests/cli/dapGeneratedCommands.test.ts"
      provides: "Generated inventory and command registration coverage"
    - path: "tests/integration/fakeAdapterCli.test.ts"
      provides: "Deterministic scripted CLI coverage for Phase 2 features"
    - path: "src/testing/dapScript.ts"
      provides: "Reusable fake-adapter script helpers for request/response scenarios"
  key_links:
    - from: "tests/cli/dapGeneratedCommands.test.ts"
      to: "src/generated/dapCommandRegistry.ts"
      via: "registry inventory assertions"
      pattern: "dapGeneratedCommands"
    - from: "tests/integration/fakeAdapterCli.test.ts"
      to: "tests/fixtures/fake-adapter-entry.ts"
      via: "scripted adapter behavior"
      pattern: "script"
---

<objective>
Lock Phase 2 behavior with deterministic generated inventory and scripted fake-adapter coverage.

Purpose: TEST-02 and TEST-05 require tests that fail on missing official DAP requests and exercise every implemented command path without relying on real JavaScript/Python adapters or event streaming.
Output: Inventory tests, alias/generated command tests, fake-adapter scripts for success and failure scenarios, and final architecture gates preserving Phase 2 boundaries.
</objective>

<must_haves>
- Truth: D-01 is satisfied because expected generated inventory comes from official DAP metadata in tests.
- Truth: D-02 is satisfied because regeneration is checked with `git diff --exit-code src/generated/dapCommandRegistry.ts`.
- Truth: D-03 is satisfied because tests prove aliases and generated commands converge on generated metadata and `dap.request`.
- Truth: D-04 is satisfied because raw request passthrough remains covered as an escape hatch.
- Truth: D-05 is satisfied because scripted tests cover breakpoints, inspection, evaluation, source, and execution-control aliases.
- Truth: D-06 is satisfied because tests assert JSON envelope, handled error, session targeting, and controller IPC contracts.
- Truth: D-07 is satisfied because unsupported capability failures are tested as structured handled JSON.
- Truth: D-08 is satisfied because capability reporting and request/session/adapter diagnostic fields are asserted.
- Truth: D-09 is satisfied because inventory tests fail on missing official DAP requests.
- Truth: D-10 is satisfied because deterministic fake-adapter scripts cover representative success, unsupported capability, paused/unpaused, raw passthrough, and alias behavior.
- Artifact: tests/cli/dapGeneratedCommands.test.ts provides generated inventory coverage.
- Artifact: tests/integration/fakeAdapterCli.test.ts provides deterministic command-path coverage.
- Key link: fake adapter fixtures simulate adapter responses without adding Phase 3 JavaScript/Python adapter support.
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add official-schema generated inventory coverage</name>
  <read_first>
    - tests/cli/dapGeneratedCommands.test.ts
    - src/generator/dapCommandRegistryGenerator.ts
    - src/generated/dapCommandRegistry.ts
    - tests/architecture/moduleBoundaries.test.ts
    - package.json
    - .planning/phases/02-complete-typed-dap-command-surface/02-RESEARCH.md
  </read_first>
  <action>
    Implement tests/cli/dapGeneratedCommands.test.ts with an inventory oracle that does not call the generator's production extraction function. Fetch/read the official schema independently in the test, traverse schema definitions directly for `*Request` entries, extract each `properties.command.enum[0]`, record the independently extracted request count, and compare that command list to `dapGeneratedCommands.map(entry => entry.command)`. Fail on missing, extra, or duplicate command entries. Add tests that generated CLI names are unique, stable sorted, generated argument validation metadata exists for representative requests with required arguments, reverse requests are marked `direction: "adapterToClient"`, and expected representative commands are present: `initialize`, `setBreakpoints`, `threads`, `stackTrace`, `scopes`, `variables`, `evaluate`, `source`, `continue`, `pause`, `next`, `stepIn`, and `stepOut`. Extend architecture tests to ensure generated command behavior lives in src/cli/commands/dapGenerated.ts and not in the generated artifact. This implements D-01, D-02, D-09, DAP-03, and TEST-02.
  </action>
  <acceptance_criteria>
    - `npm test -- tests/cli/dapGeneratedCommands.test.ts tests/architecture/moduleBoundaries.test.ts` passes.
    - `rg "setBreakpoints|stackTrace|stepIn|stepOut|dapGeneratedCommands" tests/cli/dapGeneratedCommands.test.ts` finds representative inventory assertions.
    - `rg "properties\.command\.enum|direction|argsRequired|requiredProperties|propertyTypes" tests/cli/dapGeneratedCommands.test.ts` proves the test uses an independent schema traversal and asserts generated validation/direction metadata.
    - Temporarily removing one generated registry entry causes tests/cli/dapGeneratedCommands.test.ts to fail with a missing command assertion; restore the entry before completing.
    - `npm run generate:dap-commands && git diff --exit-code src/generated/dapCommandRegistry.ts` passes.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend fake-adapter scripts for Phase 2 request coverage</name>
  <read_first>
    - src/testing/dapScript.ts
    - src/testing/fakeAdapter.ts
    - tests/fixtures/fake-adapter-entry.ts
    - tests/protocol/fakeAdapter.test.ts
    - tests/integration/fakeAdapterCli.test.ts
    - .planning/phases/02-complete-typed-dap-command-surface/02-PATTERNS.md
  </read_first>
  <action>
    Extend the fake-adapter scripting utilities to support deterministic Phase 2 request/response scenarios for setBreakpoints, threads, stackTrace, scopes, variables, source, evaluate, continue, pause, next, stepIn, stepOut, and one unsupported gated request. Keep fake-adapter scripts language-neutral and do not add JavaScript or Python adapter support. Add fixtures that can represent paused and unpaused states through scripted responses and DAP events already supported by Phase 1. Preserve existing fake adapter tests and update tests/protocol/fakeAdapter.test.ts if helper contracts change.
  </action>
  <acceptance_criteria>
    - `rg "setBreakpoints|stackTrace|variables|evaluate|stepIn|stepOut" src/testing tests/fixtures tests/protocol tests/integration` finds scripted Phase 2 request coverage.
    - `npm test -- tests/protocol/fakeAdapter.test.ts` passes.
    - `npm test -- tests/integration/fakeAdapterCli.test.ts` passes.
    - `rg "javascript|python|debugpy|js-debug" src/testing tests/fixtures` returns no new Phase 2 adapter-specific matches beyond existing unrelated text.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cover raw passthrough, capabilities, aliases, and error contracts end to end</name>
  <read_first>
    - tests/integration/fakeAdapterCli.test.ts
    - tests/helpers/runCli.ts
    - src/cli/commands/dapCore.ts
    - src/cli/commands/dapGenerated.ts
    - src/cli/commands/dapAliases.ts
    - src/controller/server.ts
    - .planning/phases/02-complete-typed-dap-command-surface/02-CONTEXT.md
  </read_first>
  <action>
    Add deterministic integration tests for every Phase 2 user-facing path: raw `request threads --json '{}'`, generated typed request command for at least one representative generated command, `capabilities`, `breakpoints set`, `threads`, `stack`, `scopes`, `variables`, `source`, `evaluate`, `continue`, `pause`, `next`, `step-in`, and `step-out`. Assert each success returns one stdout JSON envelope with `ok:true`, the expected command metadata, and empty stderr. Add handled failure tests for invalid JSON, unsupported gated capability, and paused/unpaused edge behavior where the fake adapter returns a DAP failure; assert nonzero exit, `ok:false`, request/session/adapter context, and empty stderr. This completes D-04 through D-08 and D-10.
  </action>
  <acceptance_criteria>
    - `npm test -- tests/integration/fakeAdapterCli.test.ts tests/cli/errorContracts.test.ts tests/cli/dapGeneratedCommands.test.ts` passes.
    - `rg "capabilities|breakpoints set|step-in|step-out|dap_request_unsupported|Invalid JSON" tests/integration/fakeAdapterCli.test.ts tests/cli` finds end-to-end coverage for success and failure paths.
    - `npm run check` passes.
    - Handled failure tests assert stderr is empty and stdout contains a single JSON object for invalid JSON, unsupported capability, and adapter DAP failure cases.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run `npm run generate:dap-commands`, `git diff --exit-code src/generated/dapCommandRegistry.ts`, `npm test -- tests/cli/dapGeneratedCommands.test.ts tests/integration/fakeAdapterCli.test.ts tests/protocol/fakeAdapter.test.ts tests/architecture/moduleBoundaries.test.ts`, and final `npm run check`.
</verification>

<success_criteria>
TEST-02 and TEST-05 are satisfied: generated command inventory is checked against official DAP metadata, and deterministic scripted tests exercise raw passthrough, generated commands, capabilities, aliases, unsupported capabilities, paused/unpaused behavior, and Phase 1 JSON/error/session contracts.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Official schema parser -> test oracle | External protocol metadata determines expected command coverage. |
| Fake-adapter script -> integration assertions | Scripted adapter responses simulate external adapter behavior. |
| CLI failure paths -> agent parser | Error envelopes must stay machine-readable and stderr-empty for handled failures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering | tests/cli/dapGeneratedCommands.test.ts | mitigate | Derive expected inventory from official schema parser and assert missing, duplicate, and extra generated commands. |
| T-02-04-02 | Spoofing | fake-adapter scripts | accept | Fake adapters are test-only local fixtures; tests must label scripted capabilities/responses explicitly. |
| T-02-04-03 | Repudiation | integration failure assertions | mitigate | Assert handled failures include request command, session ID, adapter diagnostics, one stdout JSON envelope, nonzero exit, and empty stderr. |
| T-02-04-04 | Denial of Service | npm run check | mitigate | Keep tests deterministic and fake-adapter based; do not add network-dependent runtime tests except explicit generator/inventory checks. |
</threat_model>
