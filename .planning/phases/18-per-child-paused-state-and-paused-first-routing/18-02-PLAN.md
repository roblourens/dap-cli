---
phase: 18-per-child-paused-state-and-paused-first-routing
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - docs/AGENT-WORKFLOWS.md
  - skills/dap-cli/SKILL.md
  - skills/dap-cli/references/javascript-typescript.md
  - tests/cli/docsValidation.test.ts
autonomous: false
requirements: [PAUSED-DOC-01]
tags: [docs, skill, child-sessions, multi-process]
must_haves:
  truths:
    - "docs/AGENT-WORKFLOWS.md's multi-process / js-debug section presents 'parent status mirrors child stops' as a guarantee, not a hedge. The section explains in one short paragraph that for multi-child js-debug attaches (pwa-node multi-process, Electron sub-Node helpers including the extension host, pwa-chrome page children) dap-cli rolls per-child paused state into the parent — agents target the parent name for status / stack / scopes / variables / evaluate / continue / pause / step* and dap-cli routes thread-bearing requests to whichever child is actually paused on the requested thread id."
    - "skills/dap-cli/SKILL.md (the in-repo plugin skill) is updated in lock-step. Any prior wording that warned the agent about parent paused-state being unreliable in multi-process attaches, or instructed the agent to discover children to find the paused one, is removed. The Core model bullet for multi-process js-debug becomes a single sentence: agents target the parent — `--thread-id` auto-resolves to the paused thread, dap-cli routes to the paused child."
    - "skills/dap-cli/references/javascript-typescript.md adds (or updates) a short 'Multi-process js-debug' subsection that names the concrete cases (pwa-node multi-process workers, Electron sub-Node helpers including the extension host / pty host / shared / file watcher / search, worker threads, pwa-chrome page children) and confirms: target the parent for inspection, no `--child-session-id` flag is needed or exists for these. The subsection links back to AGENT-WORKFLOWS.md for the long form."
    - "Phase 15-02's `child_session_not_targetable` contract for `events --name <child>` remains documented and is still correct — that's the per-child event view, not the inspection path. Make sure no doc edit accidentally contradicts it."
    - "README.md is read for any wording that overlaps these claims; update only if it carries a multi-process recipe at this granularity. If README does not currently cover it, leave the file alone — do not invent prose just to mirror."
    - "A new test case in tests/cli/docsValidation.test.ts pins the new wording: docs/AGENT-WORKFLOWS.md, skills/dap-cli/SKILL.md, and skills/dap-cli/references/javascript-typescript.md must each contain the literal phrase 'paused child' (case-insensitive). The phrase appears in the new docs as 'dap-cli routes to the paused child'. Existing docsValidation gates from prior phases are preserved."
  artifacts:
    - path: "docs/AGENT-WORKFLOWS.md"
      provides: "Authoritative multi-process js-debug recipe under the corrected paused-state union model."
      contains: "paused child"
    - path: "skills/dap-cli/SKILL.md"
      provides: "Top-level skill aligned with the new guarantee — no hedge language about parent paused state."
      contains: "paused child"
    - path: "skills/dap-cli/references/javascript-typescript.md"
      provides: "Language-specific note that multi-child js-debug attaches surface stops on the parent."
      contains: "paused child"
    - path: "tests/cli/docsValidation.test.ts"
      provides: "Grep gate pinning the phrase across the three doc files."
      contains: "paused child"
  key_links:
    - from: "skills/dap-cli/SKILL.md"
      to: "skills/dap-cli/references/javascript-typescript.md"
      via: "Core model multi-process bullet links into the JS/TS reference for specifics"
      pattern: "references/javascript-typescript"
    - from: "tests/cli/docsValidation.test.ts"
      to: "skills/dap-cli/SKILL.md"
      via: "fs.readFileSync + case-insensitive regex on 'paused child'"
      pattern: "skills/dap-cli/SKILL.md"
---

<objective>
Land the agent-facing docs change that backs 18-01's behavioural fix.
After this plan, no doc, README, or in-repo skill warns the agent that
parent paused-state may not reflect a child stop in multi-process
js-debug — because after 18-01, it does. The canonical recipe stays
"target the parent" and that recipe now actually works for the
extension-host / pwa-node multi-process / pwa-chrome multi-child
cases that previously fell off the cliff (Phase 17 / S-02).

Output: edited docs + skill files + a docsValidation grep gate that
locks the new wording so a future doc drift can't reintroduce the old
hedge.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-per-child-paused-state-and-paused-first-routing/CONTEXT.md
@.planning/phases/18-per-child-paused-state-and-paused-first-routing/18-01-PLAN.md

@docs/AGENT-WORKFLOWS.md
@README.md
@skills/dap-cli/SKILL.md
@skills/dap-cli/references/javascript-typescript.md
@tests/cli/docsValidation.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs/AGENT-WORKFLOWS.md multi-process section</name>
  <files>docs/AGENT-WORKFLOWS.md</files>
  <action>
    Find the multi-process / child-sessions section. Replace any hedge language (e.g. "parent's status.paused mirrors child stops in most cases", "if status reports paused=false try sessions --show-children") with a clear guarantee:

    > For multi-process js-debug attaches (pwa-node multi-process workers, Electron sub-Node helpers including the extension host / pty host / shared / file watcher / search, worker threads, pwa-chrome page children), dap-cli rolls per-child paused state into the parent. `dap-cli status --name <parent>` reports `paused: true` whenever any child is stopped, and stays paused even when a sibling bootloader child terminates afterwards. `--thread-id` auto-resolves to the stopped thread; `dap-cli stack` / `scopes` / `variables` / `evaluate` / `continue` / `pause` / `step*` route to the paused child automatically. Agents do not need to discover children for this case.

    Preserve the non-compact / direct-child note (Phase 15-02): for the rare case where a debug adapter spawns a child you specifically want to address as a separate session, `events --name <child>` still returns the structured `child_session_not_targetable` error pointing at the parent.
  </action>
  <verify>The new wording reads naturally; the Phase 15-02 note remains correct; the page no longer instructs agents to discover children for the common multi-child case.</verify>
</task>

<task type="auto">
  <name>Task 2: Update skills/dap-cli/SKILL.md (in-repo plugin)</name>
  <files>skills/dap-cli/SKILL.md</files>
  <action>
    Update the Core model bullet covering multi-process / child sessions to (substantially) the following one sentence + a one-line follow-up:

    > js-debug spawns child sessions for pwa-node multi-process, Electron sub-Node helpers (extension host, pty host, etc.), and pwa-chrome page children. Target the parent name — `dap-cli` rolls per-child paused state up into the parent and routes thread-bearing requests to the paused child. `--thread-id` auto-resolves.

    Remove any prior wording that hedged about parent paused-state, suggested falling back to `sessions --show-children` to find the paused child, or warned about `child_session_not_targetable` as part of the common-case workflow.

    Keep the file short — do not balloon it back up.
  </action>
  <verify>SKILL.md reads naturally; line count stays close to current; no internal contradictions with AGENT-WORKFLOWS.md.</verify>
</task>

<task type="auto">
  <name>Task 3: Update skills/dap-cli/references/javascript-typescript.md</name>
  <files>skills/dap-cli/references/javascript-typescript.md</files>
  <action>
    Add or update a short "Multi-process js-debug" subsection. It should:
    - Name the concrete cases (pwa-node multi-process workers, Electron sub-Node helpers including extension host, worker threads, pwa-chrome page children).
    - Confirm: target the parent for inspection. dap-cli routes to the paused child.
    - There is no `--child-session-id` flag for inspection; do not look for one.
    - Cross-link to docs/AGENT-WORKFLOWS.md for the long form.
    - Brief breakpoint-path note: prefer the `.ts` source path; verification is asynchronous (Phase 17 rule). The bp may verify against the child's mapped `.js` once the child loads the source.
  </action>
  <verify>The page is consistent with SKILL.md and AGENT-WORKFLOWS.md.</verify>
</task>

<task type="auto">
  <name>Task 4: Update README.md if it carries an overlapping recipe</name>
  <files>README.md</files>
  <action>
    Search README.md for any prose about multi-process / child-session paused state or `sessions --show-children` as part of an inspection recipe. If found and it overlaps these claims, update it in lock-step with AGENT-WORKFLOWS.md (one paragraph; do not duplicate). If README does not cover this at that depth, do not invent new prose.
  </action>
  <verify>README is either updated in lock-step or untouched — never partially out of sync.</verify>
</task>

<task type="auto">
  <name>Task 5: Add docsValidation grep gate</name>
  <files>tests/cli/docsValidation.test.ts</files>
  <action>
    Add a single new test case that asserts the literal phrase `paused child` (case-insensitive) appears in:
      - docs/AGENT-WORKFLOWS.md
      - skills/dap-cli/SKILL.md
      - skills/dap-cli/references/javascript-typescript.md

    Use the existing `fs.readFileSync` + regex pattern already in this file. Preserve every existing docsValidation gate (from phases 14, 15, 16). Do not change any other behavior in the file.
  </action>
  <verify>npm test -- tests/cli/docsValidation.test.ts passes; the gate fails if any of the three files loses the phrase.</verify>
</task>

</tasks>

<security>
Threats considered:
- T-18-05: Doc drift between SKILL.md, AGENT-WORKFLOWS.md, README.md, and the JS/TS reference reintroduces the stale hedge or the "use sessions --show-children for the common case" workflow. Mitigation: the new docsValidation gate fails on removal of "paused child" from any of the three primary files.
- T-18-06: A future agent reads a stale cached copy of a previously-installed user-level skill copy. Mitigation: the in-repo plugin skill is the source of truth (per the user's recent decision); the user-level copy is no longer maintained by this phase.
</security>
