---
phase: 05-stabilize-real-chrome-js-debug-playwright-same-browser-hando
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/playwrightInterop.test.ts
  - tests/fixtures/simple-chrome-page/index.html
  - tests/fixtures/simple-chrome-page/app.js
  - docs/PLAYWRIGHT-INTEROP.md
  - .planning/phases/05-stabilize-real-chrome-js-debug-playwright-same-browser-hando/05-VERIFICATION-NOTES.md
autonomous: true
requirements: [TEST-07]
must_haves:
  truths:
    - "D-04: The real-browser scenario proves Playwright and js-debug share the same browser target"
    - "D-05: Environment-sensitive real browser coverage is gated with deterministic skip diagnostics instead of silently flaking"
    - "Phase 5 preserves the Phase 4 scripted interop test as the default coordination baseline"
  artifacts:
    - path: "tests/integration/playwrightInterop.test.ts"
      provides: "Real Chrome/js-debug same-browser interop attempt or gated smoke beside scripted baseline"
      min_lines: 180
    - path: "docs/PLAYWRIGHT-INTEROP.md"
      provides: "Documented same-browser Playwright/js-debug handoff pattern and prerequisites"
      min_lines: 120
    - path: ".planning/phases/05-stabilize-real-chrome-js-debug-playwright-same-browser-hando/05-VERIFICATION-NOTES.md"
      provides: "Evidence, skip rationale if any, cleanup results, and follow-up notes"
      min_lines: 50
  key_links:
    - from: "tests/integration/playwrightInterop.test.ts"
      to: "tests/integration/jsDebugAdapter.test.ts"
      via: "reuses real js-debug setup and environment gating conventions"
      pattern: "js-debug|DAP_CLI_RUN"
    - from: "tests/integration/playwrightInterop.test.ts"
      to: "tests/fixtures/simple-chrome-page/"
      via: "serves and controls browser fixture through Playwright"
      pattern: "simple-chrome-page|calculate"
---

<objective>
Stabilize a real Chrome/js-debug same-browser Playwright handoff so dap-cli can inspect the same browser target that Playwright controls, or produce an opt-in extended smoke with precise skip diagnostics if default-runnable coverage is not yet reliable.
</objective>

<context>
@.planning/phases/05-stabilize-real-chrome-js-debug-playwright-same-browser-hando/05-CONTEXT.md
@.planning/phases/05-stabilize-real-chrome-js-debug-playwright-same-browser-hando/05-RESEARCH.md
@.planning/phases/05-stabilize-real-chrome-js-debug-playwright-same-browser-hando/05-PATTERNS.md
@.planning/phases/05-stabilize-real-chrome-js-debug-playwright-same-browser-hando/05-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Spike same-browser ownership strategy</name>
  <read_first>
    - tests/integration/playwrightInterop.test.ts
    - tests/integration/jsDebugAdapter.test.ts
    - src/config/launchConfig.ts
    - src/adapters/builtins/jsDebug.ts
  </read_first>
  <files>tests/integration/playwrightInterop.test.ts</files>
  <action>
Try Strategy A first: launch Playwright Chromium with a known remote debugging port and dedicated user data dir, then attach js-debug through dap-cli to that port. Verify that dap-cli can initialize the session and bind a breakpoint in `tests/fixtures/simple-chrome-page/app.js`.

If Strategy A is blocked by attach configuration or source mapping, record the exact failure in notes and try Strategy B: launch through js-debug with a known remote debugging port and connect Playwright with `chromium.connectOverCDP`.

Keep the existing scripted Playwright interop test intact throughout this spike.
  </action>
  <verify>
    <automated>npm test -- tests/integration/playwrightInterop.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - One same-browser strategy is selected with evidence.
    - Failure mode for rejected strategy is documented in implementation notes or verification notes.
    - Existing scripted interop test still passes.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement real-browser interop smoke</name>
  <read_first>
    - .planning/phases/05-stabilize-real-chrome-js-debug-playwright-same-browser-hando/05-VALIDATION.md
    - tests/fixtures/simple-chrome-page/index.html
    - tests/fixtures/simple-chrome-page/app.js
  </read_first>
  <files>tests/integration/playwrightInterop.test.ts, tests/fixtures/simple-chrome-page/index.html, tests/fixtures/simple-chrome-page/app.js</files>
  <action>
Add a focused test case for the selected strategy. The test must:

1. Start an isolated dap-cli controller and localhost fixture server.
2. Establish same-browser ownership between Playwright and js-debug.
3. Set or bind a breakpoint in code that only executes after debugger setup.
4. Trigger the page behavior with Playwright.
5. Poll dap-cli for `stopped`, then inspect threads, stack, scopes, and variables.
6. Continue execution and verify the Playwright-visible result.
7. Stop the session, close browser resources, and clean temp dirs.

If the real browser handoff is not stable on all machines, gate it behind `DAP_CLI_RUN_CHROME_PLAYWRIGHT_HANDOFF=1` with a clear skip message and keep the existing scripted interop test default-runnable.
  </action>
  <verify>
    <automated>npm test -- tests/integration/playwrightInterop.test.ts</automated>
    <automated>DAP_CLI_RUN_CHROME_PLAYWRIGHT_HANDOFF=1 npm test -- tests/integration/playwrightInterop.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Real handoff test exists and either runs by default or skips deterministically behind env gating.
    - The test proves same-browser ownership, not two independent browser sessions.
    - Cleanup is explicit and scoped to owned resources.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update docs and verification notes</name>
  <read_first>
    - docs/PLAYWRIGHT-INTEROP.md
    - .planning/phases/05-stabilize-real-chrome-js-debug-playwright-same-browser-hando/05-VALIDATION.md
  </read_first>
  <files>docs/PLAYWRIGHT-INTEROP.md, .planning/phases/05-stabilize-real-chrome-js-debug-playwright-same-browser-hando/05-VERIFICATION-NOTES.md</files>
  <action>
Update the Playwright interop guide with the proven same-browser handoff path, including prerequisites, command sequence, and known failure modes. Create `05-VERIFICATION-NOTES.md` with:

- Selected strategy and rationale.
- Verification commands and results.
- Whether the real handoff is default-runnable or env-gated.
- Cleanup check results.
- Any residual follow-ups that should stay deferred to Phases 6-8.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-stabilize-real-chrome-js-debug-playwright-same-browser-hando/05-VERIFICATION-NOTES.md</automated>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - Docs match implemented behavior and do not overclaim default coverage.
    - Verification notes include evidence and cleanup results.
    - Full check passes.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-browser-ownership-01 | Spoofing | Playwright/js-debug handoff | mitigate | Assert same-browser ownership through shared CDP endpoint, port, page URL, or observable page state before relying on debugger inspection. |
| T-05-flake-02 | Denial of Service | Browser interop test | mitigate | Use deterministic polling, explicit readiness checks, and env gating if platform behavior is not stable by default. |
| T-05-cleanup-03 | Denial of Service | Browser and adapter processes | mitigate | Track browser, controller, adapter, temp dir, and fixture server ownership; stop them explicitly in teardown. |
| T-05-overclaim-04 | Integrity | Documentation | mitigate | Document default-runnable versus opt-in behavior exactly as implemented and verified. |

</threat_model>

<verification>

```bash
npm test -- tests/integration/playwrightInterop.test.ts
DAP_CLI_RUN_CHROME_PLAYWRIGHT_HANDOFF=1 npm test -- tests/integration/playwrightInterop.test.ts
npm run check
ps aux | grep -E '(dap-cli-playwright|dapDebugServer|remote-debugging-port)' | grep -v grep || true
```

</verification>

<success_criteria>
- Same-browser Playwright/js-debug handoff strategy selected and documented.
- Real-browser interop smoke exists and is default-runnable or explicitly env-gated.
- Existing scripted interop test remains default-runnable.
- Docs and verification notes reflect actual behavior.
- `npm run check` passes.
- No owned browser, adapter, or controller leftovers remain after verification.
</success_criteria>

<output>
Create `05-01-SUMMARY.md` after execution with strategy outcome, verification results, and any follow-ups.
</output>
