---
phase: 05-stabilize-real-chrome-js-debug-playwright-same-browser-hando
plan: 02
type: execute
gap_closure: true
wave: 2
depends_on: [05-01]
files_modified:
  - src/sessions/activeSession.ts
  - src/sessions/sessionManager.ts
  - src/controller/client.ts
  - src/controller/server.ts
  - src/controller/requests.ts
  - tests/controller/sessionManager.test.ts
  - tests/cli/sessionCommands.test.ts
  - tests/cli/errorContracts.test.ts
  - tests/integration/selfHosting.test.ts
  - tests/integration/playwrightInterop.test.ts
  - docs/PLAYWRIGHT-INTEROP.md
  - .planning/phases/05-stabilize-real-chrome-js-debug-playwright-same-browser-hando/05-VERIFICATION-NOTES.md
autonomous: true
requirements: [TEST-07]
must_haves:
  truths:
    - "Duplicate session names are never reported as session_not_found; commands targeting an ambiguous name fail with a session_ambiguous error that lists candidate session IDs, names, and statuses."
    - "A stale or closed real js-debug session fails inspection commands with actionable diagnostics that include session ID, status, adapter log path or stderr tail when available, and cleanup/relaunch guidance."
    - "The opt-in same-browser Chrome/js-debug handoff requires a verified breakpoint binding and a real stopped event for the Playwright-triggered calculate(7, 8) call."
    - "Once the same-browser Chrome target is paused, dap-cli reads threads, stack, scopes, and variables, including left and right, then continues execution so Playwright observes result text 15."
  artifacts:
    - path: "src/sessions/activeSession.ts"
      provides: "Target resolution that distinguishes no match from ambiguous duplicate-name matches"
      contains: "session_ambiguous"
    - path: "src/sessions/sessionManager.ts"
      provides: "Session status/targeting behavior used by status, use, stop, close, events, and DAP commands"
      contains: "resolveTargetSession"
    - path: "src/controller/server.ts"
      provides: "Runtime resolution and adapter_transport_closed diagnostics for stale or closed DAP runtimes"
      contains: "adapter_transport_closed"
    - path: "tests/controller/sessionManager.test.ts"
      provides: "Unit coverage for duplicate-name ambiguity and exact-ID targeting"
      min_lines: 80
    - path: "tests/cli/sessionCommands.test.ts"
      provides: "Public CLI coverage for ambiguity errors across session commands"
      min_lines: 90
    - path: "tests/integration/selfHosting.test.ts"
      provides: "Real js-debug stop-on-entry inspection and stale/closed adapter diagnostics coverage"
      min_lines: 200
    - path: "tests/integration/playwrightInterop.test.ts"
      provides: "Opt-in same-browser Chrome breakpoint binding, stopped event, paused inspection, continue, and Playwright result assertions"
      min_lines: 220
    - path: "docs/PLAYWRIGHT-INTEROP.md"
      provides: "Documented same-browser Chrome/js-debug handoff status and prerequisites that match the verified evidence"
      contains: "same-browser"
  key_links:
    - from: "src/sessions/activeSession.ts"
      to: "tests/controller/sessionManager.test.ts"
      via: "session_ambiguous error assertions"
      pattern: "session_ambiguous|candidate"
    - from: "src/controller/server.ts"
      to: "tests/integration/selfHosting.test.ts"
      via: "adapter_transport_closed diagnostics assertions after a closed/stale js-debug runtime"
      pattern: "adapter_transport_closed|logPath|cleanupActions"
    - from: "tests/integration/playwrightInterop.test.ts"
      to: "tests/fixtures/simple-chrome-page/app.js"
      via: "verified breakpoint in calculate before Playwright triggers execution"
      pattern: "setBreakpoints|verified|calculate"
---

<objective>
Close the Phase 5 UAT gaps left after the diagnostic Chrome handoff: make session targeting failures explicit, make stale real-adapter inspection failures actionable, and convert the opt-in same-browser Chrome/js-debug test from attach-only evidence into a verified breakpoint stop plus paused inspection proof.

Purpose: Phase 5 is only complete when agents can trust dap-cli's public CLI diagnostics and can inspect the same browser that Playwright is driving.

Output: Source/test updates that satisfy the diagnosed UAT gaps, plus updated verification notes recording the new evidence.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-stabilize-real-chrome-js-debug-playwright-same-browser-hando/05-UAT.md
@.planning/phases/05-stabilize-real-chrome-js-debug-playwright-same-browser-hando/05-VALIDATION.md
@.planning/phases/05-stabilize-real-chrome-js-debug-playwright-same-browser-hando/05-VERIFICATION-NOTES.md
@.planning/phases/05-stabilize-real-chrome-js-debug-playwright-same-browser-hando/05-01-PLAN.md
@tests/integration/playwrightInterop.test.ts
@tests/integration/selfHosting.test.ts
@tests/controller/sessionManager.test.ts
@tests/cli/sessionCommands.test.ts
@tests/cli/errorContracts.test.ts
@docs/PLAYWRIGHT-INTEROP.md
@src/sessions/activeSession.ts
@src/sessions/sessionManager.ts
@src/controller/server.ts
@src/controller/client.ts
@src/controller/requests.ts

<interfaces>
From `src/sessions/activeSession.ts`:
```typescript
export interface ResolveTargetSessionOptions {
  explicitSessionId?: string | undefined;
  activeSessionId?: string | undefined;
  sessions: readonly SessionRecord[];
  requireAvailable?: boolean;
}

export function resolveTargetSession(options: ResolveTargetSessionOptions): SessionRecord;
```

From `src/sessions/session.ts`:
```typescript
export type SessionLifecycle = 'created' | 'adapterStarting' | 'transportOpen' | 'initializing' | 'initialized' | 'launching' | 'attaching' | 'configuring' | 'running' | 'stopped' | 'terminating' | 'terminated' | 'disconnected' | 'failed';
export type SessionStatusState = 'running' | 'stopped' | 'terminated' | 'unavailable' | 'failed';

export interface SessionSummary {
  id: SessionId;
  name: string;
  adapter: string;
  lifecycle: SessionLifecycle;
  status: SessionStatusState;
  updatedAt: string;
}

export interface SessionStatus extends SessionSummary {
  logPath?: string;
  stderrTail: readonly string[];
  cleanupActions: readonly string[];
}
```

From `src/controller/requests.ts`:
```typescript
export interface ControllerFailureResponse {
  id: string;
  ok: false;
  error: {
    code: string;
    message: string;
    category?: string;
    exitCode?: number;
    diagnostics?: readonly string[];
    sessionId?: string;
    request?: { command: string; seq?: number };
    adapter?: { descriptorId?: string; pid?: number; stderrTail?: readonly string[]; logPath?: string };
  };
}
```

Current UAT root causes to close:
- `resolveTargetSession` uses `matches.length !== 1` for both missing and ambiguous targets, so duplicate `node-demo` records currently produce `session_not_found`.
- `ControllerServer.resolveRuntime` and `routeDapRequest` already surface `session_unavailable` and `adapter_transport_closed`, but the diagnostics are not strong enough for stale persisted js-debug recovery.
- `tests/integration/playwrightInterop.test.ts` returns early when no stopped event is observed, so the opt-in handoff passes without proving breakpoint binding or paused inspection.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make duplicate session targeting unambiguous</name>
  <files>src/sessions/activeSession.ts, src/sessions/sessionManager.ts, src/controller/client.ts, tests/controller/sessionManager.test.ts, tests/cli/sessionCommands.test.ts, tests/cli/errorContracts.test.ts</files>
  <behavior>
    - Creating two sessions with the same `name` remains allowed unless implementation discovers an existing project convention to reject duplicate creation; targeting that shared name must fail with code `session_ambiguous`, not `session_not_found`.
    - The ambiguity error diagnostics list each candidate session with at least `id`, `name`, and projected `status`, so an agent can retry with the exact session ID.
    - Targeting either duplicate by exact session ID succeeds.
    - `status --name`, `use`, `stop --name`, `close --name`, `events --name`, common aliases such as `threads --name`, and generated commands through `dap ... --name` all receive the same ambiguity error path through shared target resolution.
  </behavior>
  <action>
Add failing tests first. Extend `tests/controller/sessionManager.test.ts` with duplicate-name cases that assert `session_ambiguous`, candidate diagnostics, and exact-ID success. Extend `tests/cli/sessionCommands.test.ts` with public JSON failure assertions for `status --name demo`, `use demo`, `stop --name demo`, `close --name demo`, `events --name demo`, one alias DAP command such as `threads --name demo`, and one generated request path such as `dap threads --name demo --json '{}'` against duplicate stored sessions. Add or update `tests/cli/errorContracts.test.ts` so failure JSON preserves the new session error code and useful diagnostics.

Implement the shared fix in `src/sessions/activeSession.ts`: split zero matches from multiple matches, keep zero matches as `session_not_found`, and throw `sessionError` with code `session_ambiguous` for multiple matches. Build diagnostics from `projectSessionSummary(candidate)` so candidate status is consistent with `sessions` output. Update `src/controller/client.ts` so `session_ambiguous` is treated as a session error when replaying controller failures in the CLI process. Keep any `SessionManager.create` duplicate-name policy conservative: do not reject duplicate creation unless tests and UAT are updated to prove that is the chosen behavior.
  </action>
  <verify>
    <automated>npm test -- tests/controller/sessionManager.test.ts tests/cli/sessionCommands.test.ts tests/cli/errorContracts.test.ts</automated>
  </verify>
  <done>
    `--name node-demo` style duplicate targeting no longer reports `session_not_found`; JSON failures have `error.code === "session_ambiguous"`, `error.category === "session"`, non-empty candidate diagnostics, and exact IDs remain targetable.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden stale real js-debug inspection diagnostics</name>
  <files>src/controller/server.ts, src/controller/requests.ts, tests/integration/selfHosting.test.ts</files>
  <behavior>
    - Fresh real js-debug Node stop-on-entry workflows continue to inspect `threads`, `stack`, `scopes`, and `variables` successfully through the public CLI.
    - When a persisted session is selected but no runtime is attached in the current controller, the CLI reports `session_unavailable` with session ID, projected status, and cleanup/relaunch guidance instead of a generic usage failure.
    - When a runtime exists but js-debug closes before an inspection response, `threads` or another inspection command reports `adapter_transport_closed` with request context, session ID, adapter descriptor, pid/log path/stderr tail when available, and guidance to run `cleanup` or relaunch by ID/name.
    - Diagnostics should not leak stack traces and should preserve the existing machine-readable JSON error contract.
  </behavior>
  <action>
Add a targeted integration test in `tests/integration/selfHosting.test.ts` for stale/closed real js-debug recovery. Reuse the existing `linkProvisionedJsDebug`, `runCli`, and controller lifecycle helpers. The test should launch a real js-debug session, reach a deterministic stopped state with `stopOnEntry`, prove normal inspection still works, then force a stale/closed path in a controlled way: either stop/restart the controller while persisted session state remains, close the adapter transport/process, or use the closest deterministic hook already available in the test helpers. Assert the public JSON failure shape for `threads --name <session>` or `threads --name <duplicate-id>`.

Implement diagnostics at the root error path. For missing runtimes, update `ControllerServer.resolveRuntime` to look up the selected session status and throw a session-category `session_unavailable` error that includes recovery guidance and available session details rather than `usageError`. For `DapTransportClosedError`, extend the existing `adapter_transport_closed` diagnostics in `toDapCliError` to include specific cleanup/relaunch guidance while preserving request, session, and adapter context. Update `ControllerFailureResponse` schema only if additional structured fields are truly needed; prefer existing `sessionId`, `request`, `adapter`, and `diagnostics` fields.
  </action>
  <verify>
    <automated>npm test -- tests/integration/selfHosting.test.ts</automated>
    <automated>npm test -- tests/cli/errorContracts.test.ts</automated>
  </verify>
  <done>
    Fresh self-hosting inspection still passes, and stale or closed real js-debug inspection failures are actionable from CLI JSON alone: the error code distinguishes `session_unavailable` from `adapter_transport_closed`, includes the session/request/adapter context available at the failure site, and tells the agent how to recover.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Require same-browser Chrome breakpoint stop and paused inspection</name>
  <files>tests/integration/playwrightInterop.test.ts, docs/PLAYWRIGHT-INTEROP.md, .planning/phases/05-stabilize-real-chrome-js-debug-playwright-same-browser-hando/05-VERIFICATION-NOTES.md</files>
  <behavior>
    - With `DAP_CLI_RUN_CHROME_PLAYWRIGHT_HANDOFF=1`, Playwright owns a persistent Chromium context with a known remote debugging port and dap-cli/js-debug attaches to that same target.
    - `breakpoints set` against `tests/fixtures/simple-chrome-page/app.js` returns breakpoint data that proves the breakpoint is verified or otherwise bound to the served script before Playwright triggers `calculate(7, 8)`.
    - The opt-in test fails when the stopped event is missing; it must not return early and count attach-only evidence as success.
    - When stopped, the test asserts `threads`, `stack`, `scopes`, and `variables`; the stack source points at `simple-chrome-page/app.js` or an equivalent served URL/source reference, variables include `left` and `right`, `continue` succeeds, and Playwright observes result text `15`.
  </behavior>
  <action>
Convert the current diagnostic branch in `tests/integration/playwrightInterop.test.ts` into a strict opt-in assertion. First, inspect the `breakpoints set` response envelope and add a schema/helper for DAP breakpoint bodies so the test can assert at least one breakpoint is returned and no unverified binding is silently ignored. Fix the Chrome attach mapping using the smallest reliable js-debug configuration change in the test setup: adjust `webRoot`, `url`, `urlFilter`, source path, breakpoint line, or served fixture URL so the filesystem breakpoint maps to the browser's loaded `app.js`. If Strategy A remains blocked after binding diagnostics, implement Strategy B from the Phase 5 notes in this same test: let js-debug launch Chrome with a known debugging port and connect Playwright with `chromium.connectOverCDP`, while preserving same-browser proof.

Remove the early `return` after `waitForStoppedEvent`. Replace it with assertions that fail with a diagnostic message containing recent event names and breakpoint verification details. Expand paused inspection assertions so they prove the real Chrome target, not the fake adapter baseline: assert positive thread/frame/scope references, source identity for `app.js`, variable names containing `left` and `right`, successful `continue`, and Playwright result text `15`. Update `docs/PLAYWRIGHT-INTEROP.md` and `05-VERIFICATION-NOTES.md` from partial diagnostic status to the actual result: complete if the opt-in command passes, or explicit blocked evidence if an external js-debug/Chromium limitation remains after strict assertions are added. The documentation must continue to avoid claiming default-runnable real-browser handoff unless the default test actually runs it.
  </action>
  <verify>
    <automated>npm test -- tests/integration/playwrightInterop.test.ts</automated>
    <automated>DAP_CLI_RUN_CHROME_PLAYWRIGHT_HANDOFF=1 npm test -- tests/integration/playwrightInterop.test.ts</automated>
    <automated>npm run check</automated>
  </verify>
  <done>
    The default Playwright interop baseline still passes, the opt-in same-browser run fails unless breakpoint binding and `stopped` are proven, and a passing opt-in run proves paused inspection plus resume in the same Chromium target Playwright controls.
  </done>
</task>

</tasks>

<risks>

| Risk | Impact | Mitigation |
|------|--------|------------|
| js-debug Chrome attach source mapping remains environment-sensitive | The opt-in Chrome handoff may still fail despite better assertions | Capture breakpoint verification details, then switch to Strategy B (`js-debug` launches Chrome, Playwright connects over CDP) within Task 3 if Strategy A cannot bind. |
| Duplicate-name diagnostics touch shared session resolution | Many commands depend on the same resolver | Keep the change centralized in `resolveTargetSession` and cover session commands, alias DAP commands, and exact-ID targeting. |
| Stale adapter tests can become flaky if they depend on process timing | Integration suite instability | Prefer deterministic controller restart or explicit adapter closure hooks over sleep-based process races; assert JSON error shape rather than exact timing. |
| Richer diagnostics may accidentally become usage-category failures | Agents lose machine-readable recovery information | Preserve `session` or `adapter` categories and add tests against `error.category`, `error.code`, `sessionId`, `request`, `adapter`, and `diagnostics`. |

</risks>

<threat_model>

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-gap-01 | Spoofing | Session name targeting | mitigate | Ambiguous names must not resolve implicitly; require exact session ID when multiple records share a name. |
| T-05-gap-02 | Repudiation | CLI diagnostics | mitigate | JSON failures must include stable error codes and candidate/recovery diagnostics so agent actions are auditable. |
| T-05-gap-03 | Denial of Service | Real js-debug stale sessions | mitigate | Surface cleanup/relaunch guidance for stale sessions and transport closure without leaving agents in retry loops. |
| T-05-gap-04 | Tampering | Chrome breakpoint mapping | mitigate | Assert breakpoint verification/binding before triggering page execution; fail if source mapping is not proven. |
| T-05-gap-05 | Integrity | Phase verification notes | mitigate | Update notes to describe only verified behavior; do not present attach-only evidence as full same-browser inspection. |

</threat_model>

<verification>

```bash
npm test -- tests/controller/sessionManager.test.ts tests/cli/sessionCommands.test.ts tests/cli/errorContracts.test.ts
npm test -- tests/integration/selfHosting.test.ts
npm test -- tests/integration/playwrightInterop.test.ts
DAP_CLI_RUN_CHROME_PLAYWRIGHT_HANDOFF=1 npm test -- tests/integration/playwrightInterop.test.ts
npm run check
```

After verification, run a scoped cleanup check for owned test leftovers only:

```bash
ps aux | grep -E '(dap-cli-playwright|dap-cli-self-hosting|dapDebugServer|remote-debugging-port)' | grep -v grep || true
```

</verification>

<acceptance_criteria>
- Duplicate session names produce `session_ambiguous` with candidate session IDs/statuses across session commands and DAP targeting paths; exact session IDs still work.
- Stale persisted sessions and closed real js-debug transports produce actionable JSON diagnostics instead of misleading `session_not_found` or opaque adapter failures.
- Fresh real js-debug Node inspection remains usable through `threads`, `stack`, `scopes`, and `variables` while stopped.
- The opt-in same-browser Chrome handoff proves breakpoint binding, observes a `stopped` event with reason `breakpoint`, inspects paused locals including `left` and `right`, continues execution, and lets Playwright observe result text `15`.
- The default Playwright interop baseline remains stable and documentation/verification notes do not overclaim behavior that is still opt-in or blocked.
- All verification commands in this plan pass, or any remaining environment limitation is recorded in `05-VERIFICATION-NOTES.md` with exact failure evidence and no false success path.
</acceptance_criteria>

<success_criteria>
- All four Phase 5 UAT gaps in `05-UAT.md` are either fixed with passing automated evidence or converted into explicit, failing/blocking evidence that cannot be mistaken for completion.
- `npm run check` passes after the gap closure implementation.
- No owned Chromium, js-debug, fixture server, controller, or fake adapter leftovers remain after the focused verification commands.
</success_criteria>

<output>
After completion, create `.planning/phases/05-stabilize-real-chrome-js-debug-playwright-same-browser-hando/05-02-SUMMARY.md` with duplicate-name diagnostics evidence, stale js-debug recovery evidence, same-browser Chrome breakpoint/inspection evidence, commands run, and any remaining external limitations.
</output>