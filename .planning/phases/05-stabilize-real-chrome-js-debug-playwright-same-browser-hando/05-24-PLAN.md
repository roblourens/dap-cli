---
phase: 05-stabilize-real-chrome-js-debug-playwright-same-browser-hando
plan: 24
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/HAND-DRIVEN-SMOKE.md
autonomous: true
gap_closure: true
requirements:
  - TEST-07
tags: [gap-closure, hand-driven, docs, H-doc]
must_haves:
  truths:
    - "`docs/HAND-DRIVEN-SMOKE.md` uses ONLY subcommand names that exist in the published CLI surface (verify with `node dist/index.js --help` and per-subcommand `--help` output, AND by cross-referencing the verbatim transcripts in `05-UAT.md` `## Hand-Driven CLI Smoke` section that succeeded)."
    - "Specifically: `start-controller` → `start`; `setBreakpoints` → `breakpoints set`; `--lines 3` → `--line 3` (singular); `stackTrace` → `stack`; `stop-controller` → whatever the actual stop-the-controller command is (verify via --help — likely `stop` with appropriate args, or a separate `controller stop`). The chrome launch in Sequence B uses `--json '{\"webRoot\":\"...\"}'` for webRoot since `--webRoot` is not a top-level flag (per the hand-driven transcript that succeeded)."
    - "The doc includes a NEW prerequisite block: `node dist/index.js --help` must list `start`, `launch`, `breakpoints`, `events`, `status`, `stack`, `continue`, `disconnect`, `close`, `cleanup`, `sessions`. If any of these are missing, the build is wrong — re-run `npm run build`."
    - "Every command in Sequences A and B is copy-pasteable and produces output matching the `Expected verbatim signal` column (after gap-closure plans 05-17..05-23 land — the signals MUST be updated to match the new behavior: `paused: true` from H-1, `stopped` event from H-2/H-6, no orphan Chromium from H-8, etc.)."
    - "The `Expected verbatim signal` table for Sequence B is updated to require: Step 6 (events) shows a `stopped` event with reason `breakpoint`; Step 7 (`pgrep`) returns 0. These are the closure signals for H-6 and H-8 respectively and SHOULD be the contract the doc enforces going forward."
    - "The doc preserves the hard rule from `.github/copilot-instructions.md`: `## Hand-Driven CLI Smoke` heading in the phase UAT.md is the single source of truth, both sequences MUST be `result: pass` before UAT can be marked complete."
  artifacts:
    - path: "docs/HAND-DRIVEN-SMOKE.md"
      provides: "Corrected subcommand names; updated expected-signal tables; copy-pasteable commands."
      contains: "breakpoints set"
  key_links:
    - from: "Sequence A Step 3 setBreakpoints invocation"
      to: "actual `breakpoints set --line` syntax"
      via: "rewrite the command and update the expected signal"
      pattern: "breakpoints set --line"
    - from: "Sequence B Step 2 launch invocation"
      to: "use --json '{\"webRoot\":...}' (no top-level --webRoot flag)"
      via: "rewrite per hand-driven transcript that worked"
      pattern: "--json.*webRoot"
---

<objective>
Close hand-driven gap H-doc (the doc-correction sub-gap noted in the user's
gap-closure brief): `docs/HAND-DRIVEN-SMOKE.md` uses subcommand names that
are SLIGHTLY WRONG (`start-controller` instead of `start`, `setBreakpoints`
instead of `breakpoints set`, `--lines` instead of `--line`,
`stackTrace` instead of `stack`). The verbatim transcript in `05-UAT.md`
proves what actually works.

Purpose: The doc IS the contract for hand-driven verification per the rule in
`.github/copilot-instructions.md`. If the doc says `start-controller` but
that command doesn't exist, the next agent runs the wrong commands, gets
confusing errors, and either gives up or produces a misleading UAT entry.
The doc must match the published CLI exactly.

Output: Corrected `docs/HAND-DRIVEN-SMOKE.md` with verified copy-pasteable
commands and updated expected-signal tables that align with the
post-05-17..05-23 behavior.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-stabilize-real-chrome-js-debug-playwright-same-browser-hando/05-UAT.md

@docs/HAND-DRIVEN-SMOKE.md
@src/cli/program.ts
@src/cli/commands/dapCore.ts
@src/cli/commands/sessions.ts
@src/cli/commands/dapAliases.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit the published CLI surface and rewrite Sequences A + B with correct commands</name>
  <files>docs/HAND-DRIVEN-SMOKE.md</files>
  <action>
    1. Build the CLI: `npm run build`. Audit the surface:
       - `node dist/index.js --help` — capture the list of top-level subcommands.
       - For each subcommand currently used in the doc (start-controller, launch, setBreakpoints, continue, events, stackTrace, evaluate, disconnect, stop-controller, sessions, cleanup, close, status, threads, stack), run `node dist/index.js <subcommand> --help` and confirm the actual flag names. Take notes inline (no separate file needed — drive the doc edit directly from this audit).
       - Cross-reference with the verbatim transcripts in `05-UAT.md` `## Hand-Driven CLI Smoke` block: those commands DEFINITELY worked, so they're authoritative.
    2. Rewrite Sequence A:
       - Replace `start-controller` with the actual command (likely `start` per the transcript).
       - Replace `setBreakpoints --lines 3` with `breakpoints set --line 3` (the transcript proved this works).
       - Replace `stackTrace` with `stack --thread-id <id>`.
       - Replace `events recent` with `events` (subverb may have been simplified — confirm).
       - Replace `stop-controller` with the actual stop command.
       - Update the expected-signal table for Steps 2 and 7 to require `paused: true, stoppedReason: 'entry'` (post-H-1 closure) and `paused: false` respectively. Update Step 4 to require the `stopped` event to be visible in `events` output (post-H-2 closure: critical events not evicted by loadedSource).
    3. Rewrite Sequence B:
       - Same fixes as Sequence A.
       - Step 2 launch: use `--json '{"webRoot":"..."}'` for webRoot (per the transcript that worked) instead of `--webRoot`.
       - Update expected signals: Step 4 — `sessions list` may NOT show child by default after H-3 closure (note that `--show-children` exposes them). Step 5 — adds the requirement that `events --include stopped` shows a `stopped` event with reason `breakpoint` within 10s of the page action (this is the H-6 closure contract). Step 6 — adds `pgrep -lf 'remote-debugging-pipe'` returning 0 after close (H-8 closure contract).
    4. Add a new top-level `## Prerequisites` bullet asserting the published CLI surface (the list of subcommands the doc uses MUST be present in `--help`).
    5. Preserve the Recording-the-result section verbatim — it's referenced by `.github/copilot-instructions.md` and must keep the same heading + frontmatter shape.
    6. Re-read the doc top-to-bottom and dry-run every command in your head against the just-built CLI's `--help` output. Anything that doesn't match → fix.
  </action>
  <verify>
    Manually verify each rewritten command against `node dist/index.js <cmd> --help`. Documented as a comment at the top of the SUMMARY listing every command and the `--help` line that confirms it exists.
  </verify>
  <done>Doc rewritten; every command verified against actual CLI; expected-signal tables align with post-05-17..05-23 behavior.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>H-doc closure: docs/HAND-DRIVEN-SMOKE.md is copy-pasteable against the published CLI.</what-built>
  <how-to-verify>
    Pick three random commands from the rewritten doc and copy-paste them verbatim into a terminal against the built `dist/index.js`. Each must execute without `unknown command` / `unknown option` errors.

    Specifically:
    1. `npm run build`
    2. From Sequence A: copy-paste the `breakpoints set` command (with a real path substituted) — must not error with `unknown option`.
    3. From Sequence B: copy-paste the launch command with `--json '{"webRoot":...}'` — must launch (or fail only because no chrome page is up, NOT because of CLI parsing).
    4. Copy-paste the stop/teardown commands at the end of either sequence — must execute.

    Append a brief confirmation to `05-UAT.md` under `## Hand-Driven CLI Smoke (gap H-doc closure)` with `result: pass` and the list of commands tested.

    NOTE: This is intentionally NOT a full Sequence A/B re-run (those happen as part of H-1/H-2/H-3/H-6/H-8 closure plans). This checkpoint is just "the doc parses against the CLI."
  </how-to-verify>
  <resume-signal>Type "approved" when the spot-check commands all execute without parsing errors.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| docs → next agent | The doc is itself a prompt for the verification agent. Wrong commands here propagate as wrong actions. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-24-01 | T | doc commands drift from CLI | mitigate | Task 1 step (1) makes the rewrite evidence-based against `--help` output captured at edit time. Future drift is out of scope (would need a `docs-validation` test — note for follow-up). |
</threat_model>

<verification>
- Spot-check checkpoint above passes.
- The full Sequence A/B re-runs (which happen as part of H-1, H-2, H-3, H-6, H-8 closure plans) succeed using the rewritten doc.
</verification>

<success_criteria>
- Every command in HAND-DRIVEN-SMOKE.md exists in the published CLI surface.
- Expected-signal tables match the post-gap-closure behavior.
- Doc remains the canonical hand-driven contract per .github/copilot-instructions.md.
</success_criteria>

<output>
Create `.planning/phases/05-stabilize-real-chrome-js-debug-playwright-same-browser-hando/05-24-SUMMARY.md` per template.
</output>
