---
phase: 05-stabilize-real-chrome-js-debug-playwright-same-browser-hando
plan: 05
type: execute
wave: 3
depends_on: [04]
files_modified:
  - tests/integration/playwrightInterop.test.ts
autonomous: true
gap_closure: true
requirements:
  - TEST-07
must_haves:
  truths:
    - "The opt-in same-browser handoff test no longer returns early when no stopped event is observed; missing stop is a hard failure."
    - "After Playwright triggers calculate(7, 8), dap-cli observes a DAP stopped event with reason 'breakpoint' against a child session bound to tests/fixtures/simple-chrome-page/app.js."
    - "While stopped, dap-cli reads non-empty threads, the top stack frame's source path resolves to simple-chrome-page/app.js, and the local scope contains both `left` and `right`."
    - "After dap-cli continues the paused thread, Playwright observes the page result text equal to 15."
  artifacts:
    - path: "tests/integration/playwrightInterop.test.ts"
      provides: "Strict same-browser handoff coverage gated by DAP_CLI_RUN_CHROME_PLAYWRIGHT_HANDOFF=1."
      contains: "calculate"
  key_links:
    - from: "tests/integration/playwrightInterop.test.ts"
      to: "controller child-session machinery from plan 05-04"
      via: "setBreakpoints fans out to children; threads/stack/scopes/variables route through children"
      pattern: "child|listChildren|app\\.js"
---

<objective>
Re-enable the strict same-browser Playwright handoff coverage now that the
controller actually multiplexes parent/child js-debug sessions (plans 05-03,
05-04). Today the test returns early when `waitForStoppedEvent` times out,
which the previous gap-closure correctly flagged as fake-success. This plan
makes the missing stop / unbound breakpoint a hard failure and asserts the
full inspect-then-continue flow.

This closes UAT gaps 1 (test 3) and 2 (test 4).

Purpose: Final proof that the same-browser pwa-chrome flow actually pauses
in `app.js`, exposes locals `left`/`right`, and continues to the expected
result.

Output: A strict, opt-in integration test. No production code changes.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-stabilize-real-chrome-js-debug-playwright-same-browser-hando/05-UAT.md
@.planning/phases/05-stabilize-real-chrome-js-debug-playwright-same-browser-hando/05-VERIFICATION-NOTES.md
@.planning/phases/05-stabilize-real-chrome-js-debug-playwright-same-browser-hando/05-04-PLAN.md

@tests/integration/playwrightInterop.test.ts
@tests/fixtures/simple-chrome-page/app.js
@tests/fixtures/simple-chrome-page/index.html
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the opt-in same-browser handoff strict</name>
  <files>tests/integration/playwrightInterop.test.ts</files>
  <behavior>
    - Test 1 (env gate preserved): Without `DAP_CLI_RUN_CHROME_PLAYWRIGHT_HANDOFF=1`, the test still skips so `npm run check` stays green by default.
    - Test 2 (breakpoint stop required): With the gate on, the test fails (does NOT pass with a warning) if `waitForStoppedEvent` returns false. The early-return-on-timeout branch is gone.
    - Test 3 (binding asserted): After `setBreakpoints` returns, the response includes a breakpoint where `verified === true` and `source.path` resolves to the fixture's `simple-chrome-page/app.js`. If verified is false, the test fails with the captured `message` field for diagnostics.
    - Test 4 (paused-state inspection): While stopped, `threads` is non-empty, the top frame's source path matches the fixture path, and the local scope's variables include both names `left` and `right` with values matching the Playwright-supplied arguments (7 and 8).
    - Test 5 (continue + page result): After `continue`, Playwright observes the result element's textContent equal to "15" within the existing timeout.
  </behavior>
  <action>
    1. Locate the `if (await waitForStoppedEvent(...))` branch in `tests/integration/playwrightInterop.test.ts` and remove the early-return on `false`. Replace with an `assert`/`expect` that fails the test when no stopped event arrives within the existing timeout.

    2. After `setBreakpoints` returns (whether sent via the before-configurationDone hook or via an explicit call after attach), assert:
       ```ts
       expect(bp.verified).toBe(true);
       expect(bp.source?.path).toMatch(/simple-chrome-page[\\/]+app\.js$/);
       ```
       If the controller now routes setBreakpoints to children (per plan 05-04), the response shape may aggregate across children — accept either the per-child verified flag OR the aggregated one, and document the chosen interpretation in the SUMMARY.

    3. Move the existing threads/stack/scopes/variables/continue assertions OUT of the conditional branch so they always run when the gate is on. Add the `left` / `right` variable name + value assertions specified in Test 4. Keep the existing cleanup (close session, close Playwright context, stop controller, clear `DAP_CLI_HOME`).

    4. After `continue`, await the existing Playwright result-text assertion that the page result equals 15.

    5. Do NOT add a new env flag. The single `DAP_CLI_RUN_CHROME_PLAYWRIGHT_HANDOFF=1` gate continues to control opt-in execution.

    6. Update the test description / comment so a future reader understands that this test now hard-fails on missing stop, by design — link back to UAT gaps 1 & 2 and to plans 05-03 / 05-04 for the supporting machinery.
  </action>
  <verify>
    <automated>npm test -- tests/integration/playwrightInterop.test.ts</automated>
  </verify>
  <done>Default `npm test -- tests/integration/playwrightInterop.test.ts` passes with the handoff test skipped; opt-in run with `DAP_CLI_RUN_CHROME_PLAYWRIGHT_HANDOFF=1` passes end-to-end and hard-fails on any missing stop / unbound breakpoint / missing locals.</done>
</task>

</tasks>

<verification>
- `npm test -- tests/integration/playwrightInterop.test.ts` passes.
- `DAP_CLI_RUN_CHROME_PLAYWRIGHT_HANDOFF=1 npm test -- tests/integration/playwrightInterop.test.ts` passes (requires plans 05-03 and 05-04 merged).
- `grep -n 'waitForStoppedEvent' tests/integration/playwrightInterop.test.ts` shows no early-return-on-false branch remaining.
</verification>

<success_criteria>
- The same-browser handoff test now hard-fails on the previous "diagnostic" path.
- Stopped, breakpoint-bound, locals (`left`, `right`), and post-continue page result are all asserted.
- Default suite remains green; opt-in suite passes when run.
</success_criteria>

<output>
After completion, create `.planning/phases/05-stabilize-real-chrome-js-debug-playwright-same-browser-hando/05-05-SUMMARY.md`
</output>
