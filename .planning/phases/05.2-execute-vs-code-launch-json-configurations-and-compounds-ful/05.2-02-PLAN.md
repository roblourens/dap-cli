---
phase: 05.2-execute-vs-code-launch-json-configurations-and-compounds-ful
plan: 02
type: execute
wave: 2
depends_on:
  - 05.2-01
files_modified:
  - src/cli/commands/dapCore.ts
  - src/cli/humanOutput.ts
  - tests/cli/dapGeneratedCommands.test.ts
  - tests/cli/humanOutput.test.ts
  - tests/integration/fakeAdapterCli.test.ts
autonomous: true
requirements:
  - CONF-01
  - SESS-02
  - SESS-03
  - AGNT-01
  - AGNT-02
  - AGNT-03
must_haves:
  truths:
    - "Agents can choose a workspace independently of the shell cwd."
    - "Agents can list launch configurations and compounds without starting adapters."
    - "Single named launch configurations use the new resolver while preserving existing flag > JSON > named precedence."
  artifacts:
    - path: "src/cli/commands/dapCore.ts"
      provides: "`--workspace`, `--list-configs`, and resolver-backed single config launch/attach routing."
    - path: "src/cli/humanOutput.ts"
      provides: "Curated human output for launch config discovery."
    - path: "tests/integration/fakeAdapterCli.test.ts"
      provides: "CLI-level tests for workspace launch configs and passthrough."
  key_links:
    - from: "src/cli/commands/dapCore.ts"
      to: "src/config/launchConfig.ts"
      via: "load/list/resolve helpers from Plan 01"
      pattern: "loadVSCodeLaunchJson|resolveLaunchConfigEntry|listLaunchConfigEntries"
---

<objective>
Wire the resolver foundation into the public CLI for discovery and single-configuration launch/attach.

Purpose: Before compounds can launch, agents need the same `--config` path to be workspace-aware and faithful for ordinary configurations.
Output: CLI options, JSON/human discovery output, and regression tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.2-execute-vs-code-launch-json-configurations-and-compounds-ful/05.2-CONTEXT.md
@.planning/phases/05.2-execute-vs-code-launch-json-configurations-and-compounds-ful/05.2-RESEARCH.md
@.planning/phases/05.2-execute-vs-code-launch-json-configurations-and-compounds-ful/05.2-01-SUMMARY.md
@src/cli/commands/dapCore.ts
@src/cli/humanOutput.ts
@tests/integration/fakeAdapterCli.test.ts

<interfaces>
Use the new Plan 01 config resolver exports directly. The existing `startDap(output, mode, options)` flow currently builds:

```typescript
const namedConfig = await resolveNamedConfig(options.config);
const jsonConfig = parseJsonRecordOption(options.json ?? '{}');
const adapterConfig = await loadAdapterConfig(process.env.DAP_CLI_HOME);
const adapterId = resolveAdapterId(options.adapter, namedConfig, adapterConfig.launchConfigTypeMap);
const adapterFlags = mapFlagsForAdapter(adapterId, collectFlagOverrides(options));
const adapterDefaults = getAdapterDefaults(adapterConfig, adapterId, mode);
const config = {
  ...resolveLaunchConfig({ namedConfig: { ...adapterDefaults, ...namedConfig }, jsonConfig, flags: adapterFlags }),
  request: mode,
};
```

Preserve this precedence shape. Only change named-config loading/resolution and add discovery mode.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add workspace-aware discovery and single config launch routing</name>
  <files>src/cli/commands/dapCore.ts, tests/integration/fakeAdapterCli.test.ts, tests/cli/dapGeneratedCommands.test.ts</files>
  <behavior>
    - Test: `launch --workspace <dir> --config "Named Fake"` reads `<dir>/.vscode/launch.json` even when process cwd is the repo root.
    - Test: `${workspaceFolder}` values in the named config reach the fake adapter as resolved paths.
    - Test: CLI flags still override JSON and named config values.
    - Test: `attach --workspace <dir> --config "Attach Fake"` keeps `request: "attach"` and routes through the attach fake script.
  </behavior>
  <action>Add `workspace?: string` and `listConfigs?: boolean` to `DapStartCommandOptions`. Register `--workspace <path>` and `--list-configs` on both `launch` and `attach`. Resolve workspace with `path.resolve(options.workspace ?? process.cwd())`; use it for launch.json discovery and variable substitution per D-09. For a configuration entry, resolve it through Plan 01 helpers, merge adapter defaults plus named config plus `--json` plus CLI flags, then force `request: mode` exactly as the current code does. Keep `--adapter` as an explicit override. If `--config` resolves to a compound for now, return a handled `usageError` with code `compound_launch_not_wired` that Plan 04 will remove; do not partially implement compound execution here.</action>
  <verify>
    <automated>npm test -- tests/integration/fakeAdapterCli.test.ts tests/cli/dapGeneratedCommands.test.ts</automated>
  </verify>
  <done>Workspace-aware single configuration launch/attach works without regressing current fake adapter launch paths or generated command tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement --list-configs JSON and human output</name>
  <files>src/cli/commands/dapCore.ts, src/cli/humanOutput.ts, tests/integration/fakeAdapterCli.test.ts, tests/cli/humanOutput.test.ts</files>
  <behavior>
    - Test: `launch --workspace <dir> --list-configs` returns a JSON array with configuration entries `{ kind, name, type, request }` and compound entries `{ kind, name, configurations, stopAll }`.
    - Test: `attach --workspace <dir> --list-configs` returns the same discovery data and does not connect to the controller.
    - Test: human mode renders a readable `Launch Configs:` table/section with kind/name/type/request/members.
  </behavior>
  <action>In `startDap`, handle `options.listConfigs === true` before parsing adapter config or connecting to the controller. Use the Plan 01 `listLaunchConfigEntries` helper and emit through `output.success(entries, { command: 'launch configs' })` or another stable command label covered by human output tests. Add a curated renderer in `src/cli/humanOutput.ts`. Do not start the controller, create sessions, or require `--config` when listing.</action>
  <verify>
    <automated>npm test -- tests/integration/fakeAdapterCli.test.ts tests/cli/humanOutput.test.ts</automated>
  </verify>
  <done>`--list-configs` is side-effect-free, machine-readable, and readable in `--human` / `DAP_CLI_HUMAN` mode.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| CLI args -> launch resolver | User-provided workspace/config/json/flags drive adapter configuration. |
| launch discovery -> terminal output | Workspace file contents are rendered to JSON/human output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-05.2-02-01 | Spoofing | `--workspace` | accept | Explicit CLI path selection is intentional; diagnostics include the resolved path when config lookup fails. |
| T-05.2-02-02 | Information disclosure | `--list-configs` | mitigate | Discovery output includes metadata only, not full resolved configs/env values. |
| T-05.2-02-03 | Repudiation | launch/attach errors | mitigate | Preserve existing one-envelope JSON error contract and stable `CliError` categories. |
</threat_model>

<verification>
Run `npm test -- tests/integration/fakeAdapterCli.test.ts tests/cli/humanOutput.test.ts tests/cli/dapGeneratedCommands.test.ts` and `npm run typecheck`.
</verification>

<success_criteria>
Agents can list launch entries and launch/attach a single named config from any workspace without losing adapter-native fields or existing CLI precedence semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-execute-vs-code-launch-json-configurations-and-compounds-ful/05.2-02-SUMMARY.md`
</output>
