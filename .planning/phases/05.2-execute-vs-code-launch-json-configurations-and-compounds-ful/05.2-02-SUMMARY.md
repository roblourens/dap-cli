---
phase: 05.2-execute-vs-code-launch-json-configurations-and-compounds-ful
plan: 02
subsystem: cli
tags: [launch-json, workspace, list-configs, human-output, fake-adapter]
requires:
  - phase: 05.2-01
    provides: Launch.json document loading, entry listing, and configuration resolution helpers
provides:
  - Workspace-aware `launch` and `attach` named configuration routing
  - Side-effect-free `--list-configs` discovery for configurations and compounds
  - Human-readable launch config discovery table
affects: [controller, sessions, compound-launch, docs]
tech-stack:
  added: []
  patterns: [pre-controller discovery commands, resolver-backed CLI config selection, curated human tables]
key-files:
  created:
    - .planning/phases/05.2-execute-vs-code-launch-json-configurations-and-compounds-ful/05.2-02-SUMMARY.md
  modified:
    - src/cli/commands/dapCore.ts
    - src/cli/humanOutput.ts
    - tests/cli/humanOutput.test.ts
    - tests/integration/fakeAdapterCli.test.ts
    - tests/fixtures/fake-adapter-entry.ts
key-decisions:
  - "`--list-configs` returns metadata only and runs before adapter config or controller IPC."
  - "Compound selections currently fail as `compound_launch_not_wired` until controller compound orchestration is implemented."
patterns-established:
  - "CLI discovery modes should return before opening controller connections."
  - "Human output renderers use command-specific table sections for agent-readable summaries."
requirements-completed: [CONF-01, SESS-02, SESS-03, AGNT-01, AGNT-02, AGNT-03]
duration: 6min
completed: 2026-05-06
---

# Phase 05.2 Plan 02: CLI Launch Config Discovery Summary

**Workspace-aware launch/attach config selection plus side-effect-free configuration and compound discovery output**

## Performance

- **Duration:** 6 min
- **Started:** 2026-05-06T02:43:10Z
- **Completed:** 2026-05-06T02:49:09Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments

- Added `--workspace <path>` and `--list-configs` to both `launch` and `attach`.
- Routed named config selection through the Plan 01 launch.json document loader and resolver.
- Preserved existing precedence: adapter defaults plus named config, then `--json`, then CLI flags, with `request` forced to the invoked mode.
- Added a temporary `compound_launch_not_wired` usage diagnostic for compound selections.
- Added side-effect-free JSON discovery output for configurations and compounds.
- Added curated human `Launch Configs:` table output.

## Task Commits

Each task was committed atomically:

1. **Task 1 RED: Add failing workspace config routing tests** - `45e6653` (test)
2. **Task 1 GREEN: Route named configs through workspace resolver** - `74a30ea` (feat)
3. **Task 2 RED: Add failing launch config discovery tests** - `731d5d6` (test)
4. **Task 2 GREEN: List launch configurations without controller IPC** - `f27ed9e` (feat)

## Files Created/Modified

- `src/cli/commands/dapCore.ts` - Adds workspace/list options, pre-controller discovery handling, resolver-backed named config routing, and compound-not-wired diagnostic.
- `src/cli/humanOutput.ts` - Adds curated launch config discovery table rendering.
- `tests/integration/fakeAdapterCli.test.ts` - Adds CLI-level workspace launch/attach and list-configs coverage.
- `tests/cli/humanOutput.test.ts` - Adds human renderer coverage for launch config discovery.
- `tests/fixtures/fake-adapter-entry.ts` - Adds dynamic fake adapter validation scripts for workspace-resolved arguments.

## Decisions Made

- Discovery output uses the stable command label `launch configs` for both `launch --list-configs` and `attach --list-configs` because both enumerate the same launch.json document entries.
- The list output intentionally omits resolved config bodies and environment values to keep discovery metadata-only.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added dynamic fake adapter fixture validators**
- **Found during:** Task 1 (workspace-aware CLI tests)
- **Issue:** Existing fake adapter scripts could only assert static argument values, but workspace paths are generated per test run.
- **Fix:** Added `expect-workspace-launch` and `expect-workspace-attach` validators to the CLI fake adapter fixture.
- **Files modified:** `tests/fixtures/fake-adapter-entry.ts`
- **Verification:** `npm test -- tests/integration/fakeAdapterCli.test.ts tests/cli/dapGeneratedCommands.test.ts` passed.
- **Committed in:** `74a30ea`

---

**Total deviations:** 1 auto-fixed (Rule 3).
**Impact on plan:** Test harness support was necessary to prove the planned workspace-resolution behavior; no product scope changed.

## Issues Encountered

- Initial dynamic fixture validation accidentally ran against `initialize`; the focused test run caught it and the validator was narrowed to launch/attach requests before the GREEN commit.

## User Setup Required

None - no external service configuration required.

## Verification

- `npm test -- tests/integration/fakeAdapterCli.test.ts tests/cli/humanOutput.test.ts tests/cli/dapGeneratedCommands.test.ts` - passed, 50 tests.
- `npm run typecheck` - passed.

## Next Phase Readiness

Wave 3 can add persisted compound session metadata now that agents can discover compounds and single configurations from any workspace.

## Self-Check: PASSED

- Key created file exists: `.planning/phases/05.2-execute-vs-code-launch-json-configurations-and-compounds-ful/05.2-02-SUMMARY.md`.
- Key modified files exist: `src/cli/commands/dapCore.ts`, `src/cli/humanOutput.ts`, `tests/integration/fakeAdapterCli.test.ts`.
- All Plan 02 verification commands passed.

---
*Phase: 05.2-execute-vs-code-launch-json-configurations-and-compounds-ful*
*Completed: 2026-05-06*