---
phase: 05.2-execute-vs-code-launch-json-configurations-and-compounds-ful
plan: 05
type: execute
wave: 5
depends_on:
  - 05.2-04
files_modified:
  - src/testing/fakeAdapter.ts
  - tests/integration/fakeAdapterCli.test.ts
  - tests/fixtures/dap-cli-target/.vscode/launch.json
autonomous: true
requirements:
  - CONF-01
  - TEST-04
  - TEST-05
  - TEST-07
must_haves:
  truths:
    - "In-repo fixture coverage proves launch.json resolution and compound orchestration together."
    - "The fixture validates variable substitution, platform merge, passthrough fields, metadata, and stopAll behavior in one CLI path."
    - "Partial startup failure cleanup is covered through the published CLI surface."
  artifacts:
    - path: "tests/fixtures/dap-cli-target/.vscode/launch.json"
      provides: "Deterministic fixture launch configurations and compounds."
    - path: "src/testing/fakeAdapter.ts"
      provides: "Fake scripts that assert resolved launch/attach arguments for compound members."
    - path: "tests/integration/fakeAdapterCli.test.ts"
      provides: "End-to-end compound fixture regression coverage."
  key_links:
    - from: "tests/integration/fakeAdapterCli.test.ts"
      to: "tests/fixtures/dap-cli-target/.vscode/launch.json"
      via: "--workspace fixture path"
      pattern: "--workspace"
---

<objective>
Prove the full Phase 05.2 fixture path through the published CLI before real VS Code UAT.

Purpose: Unit tests prove pieces; this plan proves the user-facing CLI path that agents will actually run.
Output: Fixture launch.json, fake adapter scripts, and integration tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.2-execute-vs-code-launch-json-configurations-and-compounds-ful/05.2-CONTEXT.md
@.planning/phases/05.2-execute-vs-code-launch-json-configurations-and-compounds-ful/05.2-RESEARCH.md
@.planning/phases/05.2-execute-vs-code-launch-json-configurations-and-compounds-ful/05.2-04-SUMMARY.md
@src/testing/fakeAdapter.ts
@tests/integration/fakeAdapterCli.test.ts
@tests/fixtures/dap-cli-target/

<interfaces>
Existing fake adapter scripts can assert a subset of launch/attach arguments via `createLifecycleScript(name, mode, expectedStartArguments)`. Add dedicated scripts for compound fixture members instead of weakening existing scripts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fixture launch.json and fake adapter expectations</name>
  <files>tests/fixtures/dap-cli-target/.vscode/launch.json, src/testing/fakeAdapter.ts</files>
  <behavior>
    - Test fixture includes one single configuration, one `stopAll` compound, one `stopAll: false` compound, and one broken compound.
    - Fake scripts assert resolved arguments include workspace paths, platform-specific runtime executable, and passthrough fields such as `webRoot`, `userDataDir`, `cleanUp`, and `cascadeTerminateToConfigurations`.
  </behavior>
  <action>Create or extend `tests/fixtures/dap-cli-target/.vscode/launch.json` with JSONC-style configurations using a custom fake launch type mapped in test adapter config. Include `${workspaceFolder}`, `${workspaceFolderBasename}`, `${userHome}`, `${env:DAP_CLI_COMPOUND_FIXTURE}`, platform overlays, UI/task fields that should be stripped, and adapter-native passthrough fields. Add fake adapter scripts in `src/testing/fakeAdapter.ts` such as `expect-compound-launch-member-a`, `expect-compound-attach-member-b`, and `compound-startup-fails-after-initialize` that assert the resolved DAP launch/attach arguments.</action>
  <verify>
    <automated>npm test -- tests/integration/fakeAdapterCli.test.ts -t "launch.json compound fixture"</automated>
  </verify>
  <done>Fixture files and fake scripts express the complete resolution/orchestration behavior the integration tests need.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add published CLI compound integration tests</name>
  <files>tests/integration/fakeAdapterCli.test.ts</files>
  <behavior>
    - Test: `launch --workspace tests/fixtures/dap-cli-target --list-configs` reports configuration and compound entries.
    - Test: `launch --workspace tests/fixtures/dap-cli-target --config "Fixture Compound"` returns a compound result with two members.
    - Test: `sessions` shows targetable derived names and compound metadata for both members.
    - Test: DAP request to each derived member name reaches the expected fake runtime.
    - Test: closing one member cascades when `stopAll` is true/default and preserves peers when `stopAll: false`.
    - Test: a broken compound fails with `compound_member_start_failed`, removes started records, and leaves no active runtime/session for started peers.
  </behavior>
  <action>Use `createCliTestEnv`, `runCli`, isolated `DAP_CLI_HOME`, and explicit controller cleanup like existing integration tests. Configure custom fake adapter type mapping in the test env so fixture configs do not require real adapters. Assert JSON envelope shape, metadata, and cleanup by reading `sessions` after every close/failure case. Avoid real sleeps; when polling is needed, use short deterministic loops already established in integration tests.</action>
  <verify>
    <automated>npm test -- tests/integration/fakeAdapterCli.test.ts</automated>
  </verify>
  <done>Published CLI fixture tests prove resolution, orchestration, targetability, stopAll, and partial failure cleanup end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| fixture launch.json -> fake adapter scripts | Tests encode workspace launch semantics into adapter requests. |
| fake adapter failures -> cleanup assertions | Simulated adapter failures drive controller cleanup paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-05.2-05-01 | Tampering | fixture tests | mitigate | Assert exact JSON output and post-failure session state through public CLI commands. |
| T-05.2-05-02 | Denial of service | fake adapter scripts | mitigate | Scripts are deterministic and run under isolated `DAP_CLI_HOME` with cleanup. |
</threat_model>

<verification>
Run `npm test -- tests/integration/fakeAdapterCli.test.ts`, then `npm run typecheck`.
</verification>

<success_criteria>
An in-repo fixture proves Phase 05.2 behavior through the same `npx dap-cli launch --workspace ... --config ...` surface agents use, without relying on the local VS Code checkout.
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-execute-vs-code-launch-json-configurations-and-compounds-ful/05.2-05-SUMMARY.md`
</output>
