# Phase 05.2: Execute VS Code Launch.json Configurations and Compounds - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-05
**Phase:** 05.2-execute-vs-code-launch-json-configurations-and-compounds-ful
**Areas discussed:** Compound session model, launch.json fidelity boundary, workspace discovery and CLI shape, diagnostics and unsupported features, VS Code verification target

---

## Compound Session Model

| Question | Options Presented | User's Choice | Notes |
|----------|-------------------|---------------|-------|
| How first-class should a compound be in dap-cli once launched? | First-class group; Peer sessions only; Hybrid; You decide | First-class group | Compound metadata should be persisted enough for agents to inspect and act on the group. |
| For compound shutdown, what should happen when one member is closed or terminates? | Honor VS Code stopAll; Always cascade; Manual group close only; You decide | Honor VS Code stopAll | `stopAll: true` or omitted cascades; `false` permits peers to continue. |
| How should compound members be targeted by agents after launch? | Derived member names; Nested selectors; Session IDs only; You decide | Derived member names | Use names like `<compound>/<member>` while exposing group metadata. |
| When launching a compound, how should failures be handled if one member fails during startup? | Fail and clean up started peers; Keep successful peers; Configurable; You decide | Fail and clean up started peers | Compound launch should be clean enough for agent workflows. |

---

## Launch.json Fidelity Boundary

| Question | Options Presented | User's Choice | Notes |
|----------|-------------------|---------------|-------|
| For adapter-native fields in launch.json, what passthrough policy should Phase 05.2 lock? | Pass through by default; Curated allowlist; Adapter-specific schemas; You decide | Pass through by default | Matches Phase 3 native-config decision. |
| How should VS Code-only fields like presentation, internalConsoleOptions, and serverReadyAction be treated? | Ignore silently; Warn but continue; Error on any unsupported field; You decide | Ignore silently | UI-only fields should not block CLI launch. |
| How strict should variable substitution be when a variable cannot be resolved? | Structured error; Leave literal text; Best-effort empty string; You decide | Structured error | Fail before adapter launch. |
| For platform-specific overrides, which VS Code keys should be supported in this phase? | osx/windows/linux plus mac alias; Official keys only; All platform-like keys; You decide | osx/windows/linux plus mac alias | Include `mac` as compatibility alias. |

---

## Workspace Discovery and CLI Shape

| Question | Options Presented | User's Choice | Notes |
|----------|-------------------|---------------|-------|
| What should --workspace control? | Discovery and substitution root; Discovery only; Substitution only; You decide | Discovery and substitution root | Applies to `.vscode/launch.json` lookup and `${workspaceFolder}` values. |
| How should dap-cli behave when --config matches both a configuration and a compound name? | Error as ambiguous; Prefer configuration; Prefer compound; You decide | You decide | Research/planning should verify VS Code behavior or choose an ambiguity-safe rule. |
| What should --list-configs return? | Names plus kind and members; Names only; Full resolved configs; You decide | Names plus kind and members | Useful for agent discovery without launching. |
| Should launch/attach keep --config only, or add explicit selectors for configs vs compounds? | Keep --config, add --list-configs; Add --compound; Add --config-kind; You decide | Keep --config, add --list-configs | Keep CLI simple unless ambiguity forces otherwise. |

---

## Diagnostics and Unsupported Features

| Question | Options Presented | User's Choice | Notes |
|----------|-------------------|---------------|-------|
| How should preLaunchTask and postDebugTask behave in this phase? | Structured hard error; Warn and ignore; Ignore silently; You decide | Ignore silently | This conflicted with the roadmap text. |
| Should we keep the roadmap-locked task behavior, or intentionally change it? | Keep roadmap behavior; Change to ignore silently; Warn and continue | Change to ignore silently | Record as an intentional roadmap divergence. |
| How should unsupported variables like `${input:...}` and `${command:...}` behave? | Structured hard error; Leave literal text; Ignore containing field; You decide | You decide | Planner should define exact behavior and codes. |
| How detailed should launch/compound errors be for agents? | Precise field paths and member names; Human message plus code; Adapter stderr only; You decide | You decide | Keep compatible with existing structured CLI errors. |
| For invalid compound member references, should dap-cli validate before starting anything? | Preflight validate all members; Validate as launching; Skip missing members; You decide | You decide | Must still preserve clean compound launch semantics. |

---

## VS Code Verification Target

| Question | Options Presented | User's Choice | Notes |
|----------|-------------------|---------------|-------|
| What should be the required capstone verification for Phase 05.2? | Real VS Code compound; Repo fixture compound first; Both fixture and VS Code; You decide | Both fixture and VS Code | Deterministic coverage plus hand-driven UAT. |
| For the real VS Code compound, what must count as success? | Launch all five and pause renderer; Launch all five only; Renderer path only; You decide | Launch all five and pause renderer | Must show renderer, main, extension host, shared process, and agent host. |
| How should real VS Code environment prerequisites be handled? | Documented hard prereqs; Auto-build/setup; Skip if missing; You decide | Documented hard prereqs | UAT should record missing prereqs rather than silently skipping. |
| What should the in-repo fixture compound prove? | Resolution plus orchestration; Parsing only; Lifecycle only; You decide | Resolution plus orchestration | Cover variables, platform merge, passthrough, metadata, and `stopAll`. |

---

## The Agent's Discretion

- Resolve `--config` name collisions between configuration and compound names.
- Define unsupported `${input:...}` and `${command:...}` behavior and codes.
- Define exact structured diagnostic payloads.
- Choose the implementation strategy for compound member validation while preserving clean startup semantics.

## Deferred Ideas

None.