---
phase: 05.2-execute-vs-code-launch-json-configurations-and-compounds-ful
plan: 06
type: execute
wave: 6
depends_on:
  - 05.2-05
files_modified:
  - docs/VSCODE-CHAT-SMOKE.md
  - docs/HAND-DRIVEN-SMOKE.md
  - .planning/phases/05.2-execute-vs-code-launch-json-configurations-and-compounds-ful/05.2-UAT.md
  - README.md
autonomous: false
requirements:
  - TEST-04
  - TEST-06
  - TEST-07
  - AGNT-04
  - AGNT-05
must_haves:
  truths:
    - "The real VS Code compound capstone is documented with hard prerequisites."
    - "Hand-driven UAT records whether all five VS Code member sessions launch and the renderer pauses on chat submission."
    - "User-facing docs describe workspace launch.json discovery, list-configs, compounds, and explicit task/input exclusions."
  artifacts:
    - path: "docs/VSCODE-CHAT-SMOKE.md"
      provides: "Updated capstone flow using compound launch instead of manual attach-only setup."
    - path: ".planning/phases/05.2-execute-vs-code-launch-json-configurations-and-compounds-ful/05.2-UAT.md"
      provides: "Verbatim hand-driven CLI smoke transcript and result."
    - path: "README.md"
      provides: "Discovery of `--workspace`, `--list-configs`, and compound launch behavior."
  key_links:
    - from: "docs/VSCODE-CHAT-SMOKE.md"
      to: ".planning/phases/05.2-execute-vs-code-launch-json-configurations-and-compounds-ful/05.2-UAT.md"
      via: "same command sequence and expected signals"
      pattern: "launch --config \"VS Code\" --workspace"
---

<objective>
Document and hand-drive the real VS Code compound capstone.

Purpose: Phase 05.2 is not complete until the deterministic fixture path is backed by a real VS Code launch.json UAT or an explicit unmet-prerequisite record per D-18.
Output: Docs, UAT transcript, and final verification notes.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.2-execute-vs-code-launch-json-configurations-and-compounds-ful/05.2-CONTEXT.md
@.planning/phases/05.2-execute-vs-code-launch-json-configurations-and-compounds-ful/05.2-RESEARCH.md
@.planning/phases/05.2-execute-vs-code-launch-json-configurations-and-compounds-ful/05.2-05-SUMMARY.md
@docs/VSCODE-CHAT-SMOKE.md
@docs/HAND-DRIVEN-SMOKE.md
@README.md

<interfaces>
Repo hard rule: for any verify-work round, the orchestrator must hand-drive `docs/HAND-DRIVEN-SMOKE.md` Sequence A and B with `./bin/dap-cli` / published CLI commands and record verbatim output. This plan additionally requires the Phase 05.2 real VS Code capstone from D-17.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs for launch.json workspace and compounds</name>
  <files>README.md, docs/VSCODE-CHAT-SMOKE.md, docs/HAND-DRIVEN-SMOKE.md</files>
  <action>Update user-facing docs to describe `launch --workspace <path> --list-configs`, `launch --workspace <path> --config <name>` for both configurations and compounds, derived compound member names, `stopAll` behavior, and the explicit Phase 05.2 exclusions: `preLaunchTask`/`postDebugTask` ignored silently, `${input:...}` and `${command:...}` rejected as unsupported variables, no new adapters, no event streaming. In `docs/VSCODE-CHAT-SMOKE.md`, replace the manual Terminal A/B attach setup with the capstone command `npx dap-cli launch --config "VS Code" --workspace /Users/roblou/code/vscode`, then keep the Playwright chat/breakpoint verification flow. Keep the hard prerequisite language from D-18: built VS Code checkout, persistent user data dir, built dap-cli, Playwright CLI installed.</action>
  <verify>
    <automated>npm test -- tests/integration/docsValidation.test.ts</automated>
  </verify>
  <done>Docs match implemented launch.json behavior and do not claim task/input/command variable support.</done>
</task>

<task type="auto">
  <name>Task 2: Run repo verification before real UAT</name>
  <files>.planning/phases/05.2-execute-vs-code-launch-json-configurations-and-compounds-ful/05.2-UAT.md</files>
  <action>Run `npm run build`, `npm test -- tests/config/launchConfig.test.ts tests/integration/fakeAdapterCli.test.ts tests/cli/sessionCommands.test.ts tests/cli/humanOutput.test.ts`, and `npm run typecheck`. Create `05.2-UAT.md` with headings for automated verification and real VS Code capstone. Record exact command lines, exit codes, and concise output highlights. If typecheck exposes a known pre-existing issue, record the exact diagnostic and link to the existing deferred note; do not hide it.</action>
  <verify>
    <automated>npm run build && npm test -- tests/config/launchConfig.test.ts tests/integration/fakeAdapterCli.test.ts tests/cli/sessionCommands.test.ts tests/cli/humanOutput.test.ts</automated>
  </verify>
  <done>UAT file contains automated verification transcript sufficient to understand what passed before hand-driven VS Code UAT.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Hand-drive real VS Code compound UAT</name>
  <files>.planning/phases/05.2-execute-vs-code-launch-json-configurations-and-compounds-ful/05.2-UAT.md</files>
  <what-built>Phase 05.2 launch.json resolver, CLI discovery, compound session metadata, controller orchestration, fixture tests, and docs.</what-built>
  <action>Pause for orchestrator/user verification after automated implementation. The verifier must drive the real VS Code compound flow below and append the exact transcript to `05.2-UAT.md`; this checkpoint is not complete from indirect evidence.</action>
  <how-to-verify>
1. Confirm prerequisites from `docs/VSCODE-CHAT-SMOKE.md`: `/Users/roblou/code/vscode/out/main.js` exists, `/Users/roblou/code/vscode/out/vs/workbench/contrib/chat/browser/widget/chatWidget.js` exists, `/Users/roblou/code/vscode/.build/chat-memory-smoke/user-data` exists, `npm run build` has been run in dap-cli, and `playwright-cli --version` works.
2. From `/Users/roblou/code/dap-cli`, pre-clean: `npx dap-cli cleanup --purge || true` and `npx dap-cli stop-controller || true`.
3. Run `npx dap-cli start`.
4. Run `npx dap-cli launch --config "VS Code" --workspace /Users/roblou/code/vscode`.
5. Run `npx dap-cli sessions --human` or `DAP_CLI_HUMAN=1 npx dap-cli sessions` and verify five targetable compound member sessions: `VS Code/Launch VS Code Internal`, `VS Code/Attach to Main Process`, `VS Code/Attach to Extension Host`, `VS Code/Attach to Shared Process`, `VS Code/Attach to Agent Host Process`.
6. Set the `chatWidget.ts` breakpoint per `docs/VSCODE-CHAT-SMOKE.md`, drive chat submission with `playwright-cli`, then run `npx dap-cli events --name "VS Code/Launch VS Code Internal" --include stopped --limit 5`, `npx dap-cli threads --name "VS Code/Launch VS Code Internal"`, and `npx dap-cli stack --name "VS Code/Launch VS Code Internal" --thread-id 0 --levels 5`.
7. Record verbatim output under `## Real VS Code Compound UAT` in `05.2-UAT.md` with `result: pass` only if all five sessions appear and the renderer pauses at `ChatWidget.acceptInput`. If a prerequisite is missing, record `result: blocked` with the exact missing prerequisite per D-18.
8. Run `npx dap-cli close "VS Code/Launch VS Code Internal"`, then `npx dap-cli sessions`; verify stopAll removes the compound group.
  </how-to-verify>
  <verify>
    <automated>grep -v '^#' .planning/phases/05.2-execute-vs-code-launch-json-configurations-and-compounds-ful/05.2-UAT.md | grep -q 'Real VS Code Compound UAT' && grep -v '^#' .planning/phases/05.2-execute-vs-code-launch-json-configurations-and-compounds-ful/05.2-UAT.md | grep -Eq 'result: (pass|blocked)'</automated>
  </verify>
  <done>`05.2-UAT.md` contains a `Real VS Code Compound UAT` section with verbatim command output and `result: pass` or a D-18 prerequisite `result: blocked` record.</done>
  <resume-signal>Type "approved" only after `05.2-UAT.md` contains the real VS Code transcript and result; otherwise describe the failing or blocked step.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| local VS Code checkout -> capstone command | External local workspace launch.json starts real adapter/debuggee processes. |
| Playwright CLI -> VS Code UI | UI driver sends user-like input into an authenticated development profile. |
| UAT transcript -> repo docs | Local paths and diagnostic output are recorded in planning artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-05.2-06-01 | Information disclosure | UAT transcript | mitigate | Record command output needed for verification; do not include secrets, tokens, or auth cookies. |
| T-05.2-06-02 | Denial of service | real VS Code capstone | mitigate | Use documented cleanup commands and `stopAll` close verification. |
| T-05.2-06-03 | Spoofing | prerequisite detection | mitigate | Record missing prerequisites as blocked, not pass/skip. |
</threat_model>

<verification>
Run `npm test -- tests/integration/docsValidation.test.ts`, `npm run build`, focused tests from Task 2, and the hand-driven VS Code compound UAT checkpoint.
</verification>

<success_criteria>
Docs are accurate, automated verification is recorded, and `05.2-UAT.md` proves or explicitly blocks the real VS Code compound capstone with verbatim evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-execute-vs-code-launch-json-configurations-and-compounds-ful/05.2-06-SUMMARY.md`
</output>
