---
phase: 15-child-session-enumeration-and-event-routing-for-js-debug-pwa
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - docs/AGENT-WORKFLOWS.md
  - README.md
  - ~/.copilot/skills/dap-cli/SKILL.md
  - ~/.copilot/skills/dap-cli/references/agent-workflows.md
autonomous: true
requirements: [CHILD-DOC-01]
tags: [docs, pwa-chrome, child-sessions, skill, agent-workflows]
must_haves:
  truths:
    - "docs/AGENT-WORKFLOWS.md contains a named pwa-chrome / multi-renderer subsection that spells out the only supported workflow: (1) `dap-cli sessions --show-children --name <parent>` to discover child ids, (2) read events from the parent (`dap-cli events --name <parent>`), (3) filter by `body.child_session_id` on the consumer side, and (4) DO NOT target a child name directly — the CLI rejects it with `child_session_not_targetable` and points at the parent."
    - "README.md mentions `sessions --show-children` and the `child_session_id` filter pattern in the section that already covers child sessions (or in the agent-pipeline section if no child-sessions section exists yet) — no need for a dedicated heading, but the two terms must appear at least once each."
    - "~/.copilot/skills/dap-cli/SKILL.md mirrors the same workflow (sessions --show-children → events --name <parent> → filter by child_session_id → never target a child) so cold-start agents loading the skill see it without reading the in-repo docs."
    - "~/.copilot/skills/dap-cli/references/agent-workflows.md mirrors the same workflow so the SKILL.md `More Detail` link continues to resolve to current guidance (parity with what 14-01 already established for that file pair)."
    - "tests/integration/docsValidation.test.ts continues to pass — every newly-added `dap-cli ...` example resolves to a registered command path."
  artifacts:
    - path: "docs/AGENT-WORKFLOWS.md"
      provides: "Canonical pwa-chrome / multi-renderer workflow section using the verified plumbing from plans 15-01 and 15-02"
      contains: "child_session_id"
    - path: "README.md"
      provides: "README mention of `sessions --show-children` and the `child_session_id` filter pattern"
      contains: "child_session_id"
    - path: "~/.copilot/skills/dap-cli/SKILL.md"
      provides: "Skill-level mirror of the canonical pwa-chrome workflow"
      contains: "child_session_id"
    - path: "~/.copilot/skills/dap-cli/references/agent-workflows.md"
      provides: "Reference mirror so SKILL.md `More Detail` link stays consistent"
      contains: "child_session_id"
  key_links:
    - from: "docs/AGENT-WORKFLOWS.md"
      to: "tests/integration/docsValidation.test.ts"
      via: "every new dap-cli command in fenced shell blocks resolves to a registered command path"
      pattern: "dap-cli (sessions|events|breakpoints|status|attach|launch)"
    - from: "~/.copilot/skills/dap-cli/SKILL.md"
      to: "~/.copilot/skills/dap-cli/references/agent-workflows.md"
      via: "More Detail link (already established in plan 14-01)"
      pattern: "references/agent-workflows.md"
    - from: "docs/AGENT-WORKFLOWS.md"
      to: "src/cli/commands/sessions.ts (--show-children flag) and src/controller/childSessions.ts (mirrorChildEvent)"
      via: "documented workflow corresponds to shipped code paths verified by plans 15-01 / 15-02"
      pattern: "show-children|mirrorChildEvent"
---

<objective>
Close CHILD-DOC-01: document the only supported pwa-chrome / multi-renderer
workflow in the agent-facing surface (`docs/AGENT-WORKFLOWS.md`,
`README.md`, and the user-level `~/.copilot/skills/dap-cli/` pair). The
verified plumbing from plans 15-01 (renderer logpoint output reaches the
parent stream tagged with `child_session_id`) and 15-02 (`events --name
<child>` returns the structured `child_session_not_targetable` error) makes
this workflow honest. Phase 14 already added a one-liner that "child
sessions are not targetable" — Phase 15-03 turns that one-liner into a
concrete, runnable recipe that an agent can follow from cold start.

Purpose: the analysis2.md §2 agent failed because none of the docs / skill
mentioned `--show-children` or the `child_session_id` filter pattern.
Adding both — in all four files — kills that footgun.

Output: four updated markdown files, plus a green `docsValidation.test.ts`.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-child-session-enumeration-and-event-routing-for-js-debug-pwa/CONTEXT.md
@analysis2.md

<!-- Phase 14-01 already restructured these four files to mention child
     sessions briefly. 15-03 adds the concrete recipe ON TOP of that structure
     — read the 14-01 SUMMARY first so the new content slots in with the
     existing layout instead of creating a parallel section. -->
@.planning/phases/14-update-agent-workflow-docs-and-dap-cli-usage-skill-with-less/14-01-PLAN.md

<!-- Plans 15-01 and 15-02 verified the plumbing this plan documents. Read
     their SUMMARYs before writing copy — describe what actually shipped, not
     aspirational behavior. -->
@.planning/phases/15-child-session-enumeration-and-event-routing-for-js-debug-pwa/15-01-SUMMARY.md
@.planning/phases/15-child-session-enumeration-and-event-routing-for-js-debug-pwa/15-02-SUMMARY.md

<!-- Files to edit. Read all four BEFORE writing — they have established
     formatting conventions (heading levels, fenced-block syntax, callout
     style) that the new content must match. -->
@docs/AGENT-WORKFLOWS.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical pwa-chrome workflow section to in-repo docs</name>
  <files>docs/AGENT-WORKFLOWS.md, README.md</files>
  <action>
    In `docs/AGENT-WORKFLOWS.md`: locate the existing child-sessions mention added by plan 14-01 (the line that says child sessions are not targetable; agents set breakpoints on the parent name). Below that line, add a named subsection — match the heading level the file already uses for sibling recipes — titled along the lines of "pwa-chrome multi-renderer workflow" or "Observing renderer output via the parent". The subsection MUST contain:

      1. The discovery step: a fenced shell block running `dap-cli sessions --show-children --name <parent>` and a one-line note that the response includes each child's `parent_session_id` and `name`.
      2. The observation step: a fenced shell block running `dap-cli events --name <parent>` and a one-line note that every child event is mirrored into the parent's stream with `body.child_session_id` set to the originating child id (cite plan 15-01's verified behavior in prose, not by plan id).
      3. The filter pattern: a one-line example (jq snippet or pseudo-grep) showing how a consumer filters the parent's events by `body.child_session_id == "<child id>"` to isolate one renderer.
      4. The error-path callout: a one-line note that `dap-cli events --name <child>` (or `status --name <child>`, etc.) returns `child_session_not_targetable` with the parent id in `error.data.parentSessionId` — and that the recovery is to retry against the parent. This MUST stay consistent with the 14-01 child-session paragraph; do not duplicate it, point back to it.

    In `README.md`: extend the section that 14-01 already touched (look for the `child_session_id` / "child sessions" mention added in 14-01). Add at most a two-line addition that names `dap-cli sessions --show-children` AND the `child_session_id` filter pattern, with a link to the AGENT-WORKFLOWS.md subsection just added. README does NOT need a fenced example block — it's the entry point, not the manual.

    Constraints:
      - Every fenced shell block uses ONLY commands that resolve through the registered CLI program (so `tests/integration/docsValidation.test.ts` keeps passing). Do NOT invent flags — `--show-children` already exists on `sessions`; cross-check by running `dap-cli sessions --help` against the freshly built CLI before writing the block.
      - Do NOT undo or reword the 14-01 child-session paragraph; this plan extends it.
      - Do NOT re-document `--no-human`, the launch-vs-attach rule, or the wrong-process recipe — those belong to Phase 14 and re-writing them would create drift.
  </action>
  <verify>
    <automated>npx vitest run tests/integration/docsValidation.test.ts</automated>
  </verify>
  <done>AGENT-WORKFLOWS.md has the new pwa-chrome subsection; README.md mentions `sessions --show-children` and `child_session_id`; docsValidation.test.ts is green.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror the same workflow into the user-level dap-cli skill</name>
  <files>~/.copilot/skills/dap-cli/SKILL.md, ~/.copilot/skills/dap-cli/references/agent-workflows.md</files>
  <action>
    Locate the child-session paragraph that plan 14-01 added to `~/.copilot/skills/dap-cli/SKILL.md` and to `~/.copilot/skills/dap-cli/references/agent-workflows.md`. Add the same canonical pwa-chrome workflow as Task 1, scaled to skill style:

      - SKILL.md gets a tight version: 4–6 lines of prose + ONE fenced block that chains `sessions --show-children` and `events --name <parent>` and shows the `body.child_session_id` filter idea. Skill-style is concise — no full recipe walk-through.
      - references/agent-workflows.md gets the longer version, mirroring the AGENT-WORKFLOWS.md subsection from Task 1 closely (so the SKILL.md "More Detail" link, established in 14-01, lands the reader on equivalent content).

    Both files MUST keep the existing 14-01 layout intact (do not move, rename, or reorder existing sections). Both files MUST cross-reference each other the same way 14-01 set up.

    Constraints:
      - Same CLI-validity rule as Task 1 — every fenced command resolves through the real CLI program.
      - Do NOT introduce new YAML frontmatter keys in SKILL.md.
      - Do NOT touch the SKILL.md description / trigger phrases — they are tuned for the skill discovery system.
  </action>
  <verify>
    <automated>test -f ~/.copilot/skills/dap-cli/SKILL.md && test -f ~/.copilot/skills/dap-cli/references/agent-workflows.md && grep -q "child_session_id" ~/.copilot/skills/dap-cli/SKILL.md && grep -q "child_session_id" ~/.copilot/skills/dap-cli/references/agent-workflows.md && grep -q "show-children" ~/.copilot/skills/dap-cli/SKILL.md && grep -q "show-children" ~/.copilot/skills/dap-cli/references/agent-workflows.md</automated>
  </verify>
  <done>Both user-level skill files contain `show-children` and `child_session_id`, with the More Detail link still resolving from SKILL.md to references/agent-workflows.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → cold-start agent | Docs/skill teach the agent which commands are safe and supported; incorrect copy creates footguns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | Tampering (documentation drift) | docs/AGENT-WORKFLOWS.md and SKILL.md fenced shell blocks | mitigate | docsValidation.test.ts already validates every `dap-cli ...` invocation in `docs/AGENT-WORKFLOWS.md` against the registered CLI program; Task 1's verify gate is exactly that test. The user-level skill files are validated by the Task 2 grep gate. |
| T-15-06 | Information Disclosure | none | accept | This plan is documentation-only; no new data flow, no new secrets surface. |
</threat_model>

<verification>
- `npx vitest run tests/integration/docsValidation.test.ts` is green.
- `grep -c "child_session_id" docs/AGENT-WORKFLOWS.md README.md ~/.copilot/skills/dap-cli/SKILL.md ~/.copilot/skills/dap-cli/references/agent-workflows.md` returns a non-zero count for every file (filter out comment lines if any file uses `#`-prefixed prose: `grep -v '^#' <file> | grep -c child_session_id`).
- The 14-01 child-session paragraph in each file is still present and unmodified (sanity check via `git diff`).
</verification>

<success_criteria>
1. Cold-start agent following any one of the four files lands on the same recipe: `sessions --show-children` → `events --name <parent>` → filter by `body.child_session_id` → never target a child directly.
2. The 14-01 baseline (launch-vs-attach, wrong-process recipe, didn't-bind recipe, status-as-truth, no `--no-human`) remains intact and unmodified.
3. docsValidation.test.ts is green — every new fenced command resolves through the registered CLI.
</success_criteria>

<output>
After completion, create `.planning/phases/15-child-session-enumeration-and-event-routing-for-js-debug-pwa/15-03-SUMMARY.md`.
</output>
