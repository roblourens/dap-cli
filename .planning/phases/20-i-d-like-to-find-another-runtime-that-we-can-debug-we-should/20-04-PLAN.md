---
phase: 20-i-d-like-to-find-another-runtime-that-we-can-debug-we-should
plan: 04
type: execute
wave: 3
depends_on: [20-01, 20-02]
files_modified:
  - docs/adapter-setup.md
  - dap-cli/skills/dap-cli/SKILL.md
  - dap-cli/skills/dap-cli/references/agent-workflows.md
  - dap-cli/skills/dap-cli/references/go-delve.md
  - tests/integration/docsValidation.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Public setup docs explain Delve provisioning, PATH fallback, adapter id `delve`, type `go`, and `.go` inference with commands that match the implemented CLI."
    - "Agent-facing skill guidance covers Go launch/test/exec/local-attach recipes, Delve build-symbol advice, and when to use raw JSON overrides."
    - "Docs-validation tests pin the Phase 20 public terms so guidance cannot silently drift."
  artifacts:
    - path: "docs/adapter-setup.md"
      provides: "Go/Delve install and troubleshooting reference"
    - path: "dap-cli/skills/dap-cli/references/go-delve.md"
      provides: "Go-specific agent workflow reference"
    - path: "tests/integration/docsValidation.test.ts"
      provides: "Go/Delve public docs drift checks"
  key_links:
    - from: "docs/adapter-setup.md"
      to: "scripts/setup-adapters.ts"
      via: "`npm run setup-adapters` Delve instructions"
      pattern: "setup-adapters.*Delve|Delve.*setup-adapters"
    - from: "dap-cli/skills/dap-cli/SKILL.md"
      to: "dap-cli/skills/dap-cli/references/go-delve.md"
      via: "skill reference discoverability"
      pattern: "go-delve\\.md"
---

<objective>
Turn the Go/Delve implementation into accurate human and agent guidance, then protect that guidance with docs-validation assertions.

Purpose: The user explicitly wants fresh agents to try the new language. They cannot do that reliably if setup, launch shapes, attach lifecycle, or Delve-specific failure modes live only in source code.

Output: Updated adapter setup docs, a Go/Delve skill reference wired into the dap-cli skill/workflow index, and docs-validation coverage.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-i-d-like-to-find-another-runtime-that-we-can-debug-we-should/20-RESEARCH.md
@.planning/phases/20-i-d-like-to-find-another-runtime-that-we-can-debug-we-should/20-03-SUMMARY.md
@docs/adapter-setup.md
@dap-cli/skills/dap-cli/SKILL.md
@dap-cli/skills/dap-cli/references/agent-workflows.md
@dap-cli/skills/dap-cli/references/python.md
@dap-cli/skills/dap-cli/references/javascript-typescript.md
@tests/integration/docsValidation.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document Delve setup, Go adapter discovery, and troubleshooting in public adapter docs</name>
  <files>docs/adapter-setup.md</files>
  <read_first>
    - docs/adapter-setup.md
    - scripts/setup-adapters.ts
    - src/config/programInference.ts
    - src/config/launchConfig.ts
    - .planning/phases/20-i-d-like-to-find-another-runtime-that-we-can-debug-we-should/20-ADAPTER-SELECTION.md
    - .planning/phases/20-i-d-like-to-find-another-runtime-that-we-can-debug-we-should/20-03-SUMMARY.md
  </read_first>
  <action>
Extend `docs/adapter-setup.md` with a Go/Delve section that matches the final implementation exactly. Include:
- `delve` in the built-in adapter list and `go` / `.go` in the adapter/type inference reference;
- `npm run setup-adapters` setup/readiness expectations, Delve v1.26.3 pinned provisioning behavior, PATH fallback, and manual `go install github.com/go-delve/delve/cmd/dlv@v1.26.3` fallback only if that remains valid after implementation;
- one launch example, one test-mode or exec-mode example, and one local attach JSON example using `mode: "local"` with `processId`;
- troubleshooting for `delve_not_found`, debug-symbol-friendly exec builds using `-gcflags=all="-N -l"`, localhost-only built-in `dlv dap`, and the distinction between this built-in path and advanced remote/headless Delve.

Use command shapes and lifecycle facts from Plans 20-01 through 20-03, not research hypotheticals. Mention later verify-work hand-driven smoke only where this repo's docs already describe verification policy; do not claim it is replaced by automated tests.
  </action>
  <acceptance_criteria>
    - `grep -q 'Delve' docs/adapter-setup.md`
    - `grep -q 'delve_not_found' docs/adapter-setup.md`
    - `grep -q 'processId' docs/adapter-setup.md`
    - `grep -q 'gcflags=all="-N -l"\|gcflags=all=\\"-N -l\\"' docs/adapter-setup.md`
    - `grep -q '127.0.0.1' docs/adapter-setup.md`
  </acceptance_criteria>
  <verify>
    <automated>bash -c "grep -q 'Delve' docs/adapter-setup.md && grep -q 'delve_not_found' docs/adapter-setup.md && grep -q 'processId' docs/adapter-setup.md"</automated>
  </verify>
  <done>The adapter setup doc tells a human exactly how Go/Delve is provisioned, selected, launched, attached, and troubleshot.</done>
</task>

<task type="auto">
  <name>Task 2: Add Go/Delve agent skill guidance and wire it into the workflow index</name>
  <files>dap-cli/skills/dap-cli/SKILL.md, dap-cli/skills/dap-cli/references/agent-workflows.md, dap-cli/skills/dap-cli/references/go-delve.md</files>
  <read_first>
    - dap-cli/skills/dap-cli/SKILL.md
    - dap-cli/skills/dap-cli/references/agent-workflows.md
    - dap-cli/skills/dap-cli/references/python.md
    - dap-cli/skills/dap-cli/references/javascript-typescript.md
    - docs/adapter-setup.md
    - dap-cli/skills/dap-cli/references/go-delve.md
  </read_first>
  <action>
Create `dap-cli/skills/dap-cli/references/go-delve.md` following the existing language-reference tone and density. It must tell a fresh agent which setup readiness command to run, how to launch a Go package, how `mode: "test"` and `mode: "exec"` differ, how to build exec binaries with symbols, how to do safe local PID attach, and how to inspect paused state with dap-cli after a breakpoint hits. Include a negative-diagnostics section for missing Delve, bad program paths, symbol-poor exec binaries, and invalid attach PIDs.

Update `SKILL.md` and `agent-workflows.md` so the Go reference is discoverable in the same style as Python and JavaScript references. Keep wording concrete enough for Plan 20-06's docs-only novice scenario to hand a fresh agent these files without extra coaching.
  </action>
  <acceptance_criteria>
    - `test -f dap-cli/skills/dap-cli/references/go-delve.md`
    - `grep -q 'go-delve.md' dap-cli/skills/dap-cli/SKILL.md dap-cli/skills/dap-cli/references/agent-workflows.md`
    - `grep -q 'mode.*test' dap-cli/skills/dap-cli/references/go-delve.md`
    - `grep -q 'mode.*exec' dap-cli/skills/dap-cli/references/go-delve.md`
    - `grep -q 'processId' dap-cli/skills/dap-cli/references/go-delve.md`
  </acceptance_criteria>
  <verify>
    <automated>bash -c "test -f dap-cli/skills/dap-cli/references/go-delve.md && grep -q 'go-delve.md' dap-cli/skills/dap-cli/SKILL.md && grep -q 'processId' dap-cli/skills/dap-cli/references/go-delve.md"</automated>
  </verify>
  <done>Fresh agents can discover a dedicated Go/Delve reference from the skill entry points and use it for the hardening scenarios later in the phase.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pin Go/Delve docs terms with docs-validation tests</name>
  <files>tests/integration/docsValidation.test.ts</files>
  <read_first>
    - tests/integration/docsValidation.test.ts
    - docs/adapter-setup.md
    - dap-cli/skills/dap-cli/SKILL.md
    - dap-cli/skills/dap-cli/references/agent-workflows.md
    - dap-cli/skills/dap-cli/references/go-delve.md
  </read_first>
  <behavior>
    - Docs validation covers the new Go reference if it contains public commands.
    - Assertions fail if setup docs lose Delve setup text, `delve_not_found`, Go attach `processId`, or the skill index reference.
    - The existing command-fence parser and docs validation patterns remain the only validation machinery; no duplicate parser is introduced.
  </behavior>
  <action>
Extend `tests/integration/docsValidation.test.ts` using the existing docs list and assertion style. Add the Go/Delve reference to the validated documents when it contains dap-cli command examples, and add Phase 20-specific terms that should never drift out of docs/skills: `Delve`, `delve_not_found`, `processId`, and `go-delve.md`. Keep the checks narrow and aligned with the actual doc text written in Tasks 1 and 2.
  </action>
  <acceptance_criteria>
    - `grep -q 'go-delve.md' tests/integration/docsValidation.test.ts`
    - `grep -q 'delve_not_found' tests/integration/docsValidation.test.ts`
    - `npx vitest run tests/integration/docsValidation.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/integration/docsValidation.test.ts</automated>
  </verify>
  <done>Public Go/Delve guidance has an automated drift gate that later fixes and hardening edits must keep green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/skills → commands executed by humans and agents | Written examples become executable instructions. |
| debug examples → attach/process behavior | Guidance can cause users or agents to attach to real local PIDs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04-01 | Tampering | setup command examples | mitigate | Docs name the pinned/official Delve flow from Plan 20-01 and tests preserve exact readiness vocabulary; no arbitrary curl-pipe-shell examples. |
| T-20-04-02 | Information Disclosure | logging/troubleshooting guidance | mitigate | Docs discuss adapter logs and locals generically; they do not encourage copying private source, tokens, or full external-repo logs into public output. |
| T-20-04-03 | Elevation | local attach guidance | mitigate | Skill/docs specify safe locally owned process PID attach and process-lifetime cleanup, matching the gated test contract from Plan 20-03. |
| T-20-04-04 | Repudiation | stale docs after code changes | mitigate | `docsValidation.test.ts` pins adapter/diagnostic/reference terms so drift is visible in CI. |
</threat_model>

<verification>
- Run `npx vitest run tests/integration/docsValidation.test.ts`.
- Spot-check docs and Go skill reference against the final command shapes from Plans 20-01 through 20-03.
</verification>

<success_criteria>
- Setup docs and skill references describe the implemented Go/Delve surface accurately.
- Go debugging guidance covers launch, test, exec, attach, negative diagnostics, and paused-state inspection.
- Docs validation prevents the new Go guidance from becoming invisible or stale.
</success_criteria>

<output>
After completion, create `.planning/phases/20-i-d-like-to-find-another-runtime-that-we-can-debug-we-should/20-04-SUMMARY.md`.
</output>