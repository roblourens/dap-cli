---
phase: 05.1-a-mode-for-the-cli-where-it-produces-human-readable-nicely-f
plan: 06
type: execute
wave: 6
depends_on: [05]
files_modified:
  - src/cli/humanOutput.ts
  - tests/cli/humanOutput.test.ts
  - tests/cli/jsonOutput.test.ts
autonomous: true
requirements: [PHASE-05.1]
must_haves:
  truths:
    - "Human output no longer shows Command or Timestamp metadata lines."
    - "Table-like human output uses formatted bordered tables instead of tab-separated rows."
    - "Human output has a terminal-aware styling layer that can color useful values while preserving JSON output and respecting NO_COLOR."
  artifacts:
    - path: "src/cli/humanOutput.ts"
      provides: "Human output renderer polish"
      contains: "formatTable"
    - path: "tests/cli/humanOutput.test.ts"
      provides: "Renderer regressions for metadata removal, tables, and color behavior"
      contains: "NO_COLOR"
    - path: "tests/cli/jsonOutput.test.ts"
      provides: "CLI-level human output regression updates"
      contains: "--human success output is human text"
---

<objective>
Polish human-readable output presentation based on user feedback.

Purpose: Make `--human` feel intentionally designed for terminal reading rather than JSON with light labels.
Output: Cleaner human output without command/timestamp noise, bordered tables for tabular data, terminal-aware semantic colors, and tests that preserve the JSON contract.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove noisy metadata from human output</name>
  <files>src/cli/humanOutput.ts, tests/cli/humanOutput.test.ts, tests/cli/jsonOutput.test.ts</files>
  <action>
    Update success and failure rendering so human output does not include `Command:` or `Timestamp:` lines. Keep JSON metadata unchanged.
  </action>
  <verify>
    `npx vitest run tests/cli/humanOutput.test.ts tests/cli/jsonOutput.test.ts`
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Format table-like output with bordered tables</name>
  <files>src/cli/humanOutput.ts, tests/cli/humanOutput.test.ts</files>
  <action>
    Replace tab-separated table rows with deterministic bordered tables for sessions, events, breakpoints, threads, stack, scopes, and variables.
  </action>
  <verify>
    `npx vitest run tests/cli/humanOutput.test.ts`
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add terminal-aware semantic styling</name>
  <files>src/cli/humanOutput.ts, tests/cli/humanOutput.test.ts</files>
  <action>
    Add a small ANSI styling layer for human output that respects `NO_COLOR` and `TERM=dumb`, supports `FORCE_COLOR`, and highlights values such as booleans/statuses/errors and variable types.
  </action>
  <verify>
    `npm run check`
  </verify>
</task>

</tasks>

<success_criteria>
- Human success/failure output does not contain `Command:` or `Timestamp:`.
- Table-like outputs contain bordered tables with stable columns.
- `NO_COLOR=1` suppresses ANSI styling; `FORCE_COLOR=1` enables styling in tests.
- `npm run check` passes.
</success_criteria>