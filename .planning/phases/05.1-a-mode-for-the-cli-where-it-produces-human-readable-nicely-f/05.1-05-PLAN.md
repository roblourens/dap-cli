---
phase: 05.1
plan: 05
type: execute
wave: 5
depends_on: [04]
files_modified:
  - docs/HAND-DRIVEN-SMOKE.md
  - .planning/phases/05.1-a-mode-for-the-cli-where-it-produces-human-readable-nicely-f/05.1-UAT.md
  - tests/integration/docsValidation.test.ts
autonomous: true
requirements:
  - TEST-06
  - AGNT-05
must_haves:
  truths:
    - "The canonical hand-driven smoke sequences (A and B) in docs/HAND-DRIVEN-SMOKE.md are truthful, robust, and runnable as written by a human following the doc."
    - "Sequence A no longer hard-codes a thread id; it instructs the executor to use the thread id returned by `threads` and provides an alternative `evaluate --frame-id` assertion."
    - "Sequence B documents a safe clean-state prerequisite (`cleanup --purge`) so `sessions` output is not polluted by previously persisted demo records."
  artifacts:
    - path: "docs/HAND-DRIVEN-SMOKE.md"
      provides: "Canonical hand-driven sequences A and B with corrected commands and prerequisites"
    - path: ".planning/phases/05.1-a-mode-for-the-cli-where-it-produces-human-readable-nicely-f/05.1-UAT.md"
      provides: "Updated UAT with rerun verbatim transcripts and `result: pass` entries for sequences A and B"
    - path: "tests/integration/docsValidation.test.ts"
      provides: "Docs examples validation remains green after doc edits"
  key_links:
    - from: "docs/HAND-DRIVEN-SMOKE.md"
      to: "tests/integration/docsValidation.test.ts"
      via: "extractDapCliExamples() collects `dap-cli` examples from docs and asserts commands are registered in `createProgram()`"

---

<objective>
[Make the hand-driven smoke docs truthful and rerunnable]
Purpose: Close the two UAT gaps reported in 05.1-UAT.md by updating `docs/HAND-DRIVEN-SMOKE.md` so its canonical sequences are robust across fresh and warm local state, and by clarifying adapter provisioning notes.
Output: Updated `docs/HAND-DRIVEN-SMOKE.md`, a short UAT rerun checklist in the phase UAT file, and an updated docs validation test if necessary.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05.1-a-mode-for-the-cli-where-it-produces-human-readable-nicely-f/05.1-UAT.md
docs/HAND-DRIVEN-SMOKE.md
tests/integration/docsValidation.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs/HAND-DRIVEN-SMOKE.md Sequence A to avoid hard-coded thread id and fragile evaluate expectation</name>
  <files>docs/HAND-DRIVEN-SMOKE.md</files>
  <action>
    - Replace the Sequence A step that currently shows `node dist/index.js stack --name smoke-node --thread-id 1` with an instruction that the executor MUST use the thread id returned by `threads`. Concretely change the two places in Sequence A that reference a hard-coded `--thread-id 1` to:

      "Use the thread id returned by `threads` (for example `--thread-id 0`):
      `node dist/index.js threads --name smoke-node`
      `node dist/index.js stack --name smoke-node --thread-id <THREAD_ID_FROM_THREADS>`"

    - Replace the brittle `evaluate --expression "typeof dapCliSelfHostDemo"` expectation in the step's expected signals with a robust alternative. Update the command example to suggest using `--frame-id` when wanting to inspect the top frame, and change the expected signal to assert the stack top frame name. Suggested replacement text and commands:

      In the example commands: replace
      `node dist/index.js evaluate --name smoke-node --expression "typeof dapCliSelfHostDemo"`
      with
      `# (Optional) To inspect the top frame symbol, use the frame id from `stack`:
      node dist/index.js stack --name smoke-node --thread-id <THREAD_ID>  # note top frame id
      node dist/index.js evaluate --name smoke-node --frame-id <TOP_FRAME_ID> --expression "typeof dapCliSelfHostDemo"`

      And update the Expected verbatim signals table: instead of requiring `evaluate` to return `"function"`, require that the top frame name is `dapCliSelfHostDemo` at line 2 (observed via `stack`) and optionally that evaluating the specific `frame-id` shows a non-undefined result.

    - Preserve the rest of Sequence A; only change the hard-coded thread id and the evaluate expectation text and examples.
  </action>
  <verify>
    - `grep -n "Use the thread id returned by \`threads\`" docs/HAND-DRIVEN-SMOKE.md` must return at least one line.
    - `grep -n "evaluate --name smoke-node --frame-id" docs/HAND-DRIVEN-SMOKE.md` must return at least one line.
  </verify>
  <done>The Sequence A section of `docs/HAND-DRIVEN-SMOKE.md` no longer hard-codes `--thread-id 1` and documents the frame-id `evaluate` alternative; the two grep checks above pass.</done>
</task>

<task type="auto">
  <name>Task 2: Make Sequence B deterministic by adding a cleanup prerequisite and clarify adapter provisioning note</name>
  <files>docs/HAND-DRIVEN-SMOKE.md, .planning/phases/05.1-a-mode-for-the-cli-where-it-produces-human-readable-nicely-f/05.1-UAT.md</files>
  <action>
    - Insert a new prerequisite step (before Sequence B or at its start) instructing the human to run `node dist/index.js cleanup --purge` to remove stale persisted session records. Add brief explanation: "This ensures `sessions` output is not polluted by previously persisted demo records (e.g., `ts-button-demo`)."
    - Update the `Prerequisites` section to clarify that `debugpy` may be provisioned to `~/.dap-cli/venv` by `scripts/setup-adapters.ts` and that `ls ~/.dap-cli/adapters` may only show `js-debug` while `debugpy` lives in the repo-managed venv. Add the exact note: "If `debugpy` is not listed under `~/.dap-cli/adapters` but `scripts/setup-adapters.ts` reports provisioning to `~/.dap-cli/venv`, run the provisioning script per the doc: `npx tsx scripts/setup-adapters.ts` — the provisioned debugpy will be available to the controller via the venv path."
    - Update Sequence B expected signals table: change the `sessions` step expectation to accept either (a) an exact-clean listing after `cleanup --purge` showing only `smoke-chrome`, or (b) if the human chooses not to purge, the doc explains that stale terminated records are allowed and the check is to find the `smoke-chrome` parent row in the `sessions` output (presence, not first-only ordering). Prefer the purge path as the recommended safer default.
  </action>
  <verify>
    - `grep -n "cleanup --purge" docs/HAND-DRIVEN-SMOKE.md` must return at least one line.
    - `grep -n "provisioned to ~/.dap-cli/venv" docs/HAND-DRIVEN-SMOKE.md` must return the adapter provisioning clarification line.
  </verify>
  <done>Sequence B includes a documented `cleanup --purge` prerequisite and adapter provisioning clarification; the two grep checks above pass.</done>
</task>

<task type="auto">
  <name>Task 3: Verification instructions and test hooks</name>
  <files>.planning/phases/05.1-a-mode-for-the-cli-where-it-produces-human-readable-nicely-f/05.1-UAT.md, tests/integration/docsValidation.test.ts</files>
  <action>
    - Update `.planning/.../05.1-UAT.md` with a short checklist that the human must run: (1) execute Sequence A and B exactly as in the doc and paste verbatim transcripts under `sequences` id A and B; (2) mark `result: pass` for both sequences; (3) capture `ls ~/.dap-cli/adapters` output and `npx tsx scripts/setup-adapters.ts` output when relevant.
    - Add an automated acceptance step to the plan: after docs edits, run `npx vitest run tests/integration/docsValidation.test.ts` to ensure doc command examples remain valid. If the test fails due to doc example changes (rare), the plan includes adjusting `tests/integration/docsValidation.test.ts` to accept the updated examples (for example, if the doc now shows `node dist/index.js stack --name smoke-node --thread-id <THREAD_ID_FROM_THREADS>` in a fenced example, ensure the docs validation helper still extracts `stack` as a command; prefer non-regex-changing edits in the doc so test continues to pass without code changes).
  </action>
  <verify>
    - `npx vitest run tests/integration/docsValidation.test.ts` must exit 0. (This is part of the automated verification in the plan; human UAT must still be run afterward.)
    - `.planning/.../05.1-UAT.md` must include a new `sequences` block for A and B and instructions for the human to mark `result: pass`.
  </verify>
  <done>Docs validation test passes and UAT file contains the checklist and placeholders for verbatim transcripts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human-run smoke doc → repository | The doc instructs humans to run local commands that rely on local persisted state; stale local state can hide failures or create false positives. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-01 | Tampering | docs/HAND-DRIVEN-SMOKE.md | mitigate | Avoid hard-coded runtime IDs; instruct humans to use `threads` output. Add `cleanup --purge` to mitigate warm-state issues. |
| T-05.1-02 | Information Disclosure | UAT transcripts | accept | UAT contains local paths and PIDs; repository UAT is intended for project verification and may include local identifiers. Document that sensitive data should be redacted manually before publishing. |
</threat_model>

<verification>
- Automated: `npx vitest run tests/integration/docsValidation.test.ts` exits 0.
- Manual: Human re-runs `docs/HAND-DRIVEN-SMOKE.md` Sequence A and Sequence B in a real terminal and appends verbatim transcripts into `.planning/phases/05.1-a-mode-for-the-cli-where-it-produces-human-readable-nicely-f/05.1-UAT.md` with `result: pass` for both sequences before the phase can be marked complete.
</verification>

<success_criteria>
- `docs/HAND-DRIVEN-SMOKE.md` contains the phrases: "Use the thread id returned by `threads`" and "cleanup --purge".
- `npx vitest run tests/integration/docsValidation.test.ts` passes.
- The human UAT file contains two `sequences` entries (A and B) with `result: pass` and verbatim captured_output blocks.
</success_criteria>

<output>
After completion, update `.planning/phases/05.1-a-mode-for-the-cli-where-it-produces-human-readable-nicely-f/05.1-UAT.md` with the verbatim transcripts and mark `status: complete` and `result: pass` for sequences A and B.
</output>
