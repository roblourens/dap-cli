---
phase: 16
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - docs/AGENT-WORKFLOWS.md
  - README.md
  - docs/PLAYWRIGHT-INTEROP.md
  - /Users/roblou/.copilot/skills/dap-cli/SKILL.md
  - /Users/roblou/.copilot/skills/dap-cli/references/agent-workflows.md
  - tests/integration/docsValidation.test.ts
autonomous: true
requirements: [PYEVAL-02, VERB-DOC-01, PWDOC-01]

must_haves:
  truths:
    - "An agent reading docs/AGENT-WORKFLOWS.md learns: on debugpy sessions, `evaluate` auto-wraps statement-shaped Python in `exec(...)`, and that pure expressions are passed through unchanged. The doc also describes the `evaluate_requires_exec` fallback envelope."
    - "README.md surfaces the same Python evaluate rule in its quick reference / examples."
    - "An agent reading the user-level skill (~/.copilot/skills/dap-cli/SKILL.md and references/agent-workflows.md) gets the same Python evaluate guidance — no contradictions across surfaces."
    - "Both AGENT-WORKFLOWS.md AND the user-level skill explain the verb-selection rule (analysis2.md §1): `dap-cli launch` and `dap-cli attach` are separate top-level commands; there is no `--request` flag; Phase 10 auto-routes when a `--config` JSON `request:` field disagrees with the verb."
    - "docs/PLAYWRIGHT-INTEROP.md contains a one-paragraph note on the playwright-cli daemon-died failure mode (the `not open, please run open first` symptom) and the kill-and-reattach recovery recipe."
    - "docsValidation.test.ts (or an equivalent grep gate) enforces presence of the new keywords so the docs cannot silently rot in a later phase."
  artifacts:
    - path: docs/AGENT-WORKFLOWS.md
      provides: "New `### Python evaluate (debugpy)` subsection (or equivalent named section) under the existing evaluate guidance (~L160-180). New `### Choosing launch vs attach` short subsection."
      contains: "exec("
    - path: README.md
      provides: "One-paragraph callout in the existing dap-cli quick-reference section pointing to the AGENT-WORKFLOWS.md Python evaluate section."
      contains: "evaluate_requires_exec"
    - path: docs/PLAYWRIGHT-INTEROP.md
      provides: "New `### Recovering from a dead playwright-cli daemon` paragraph naming the symptom and the kill-and-reattach steps."
      contains: "not open, please run open first"
    - path: /Users/roblou/.copilot/skills/dap-cli/SKILL.md
      provides: "Updated `## dap-cli Notes` to reflect that auto-wrap now exists — keep the existing `BAD/GOOD` examples but add a new line: `Phase 16+: dap-cli evaluate auto-wraps detected statements in exec(...) on debugpy sessions; the manual exec() trick from this note is now a fallback.`"
    - path: /Users/roblou/.copilot/skills/dap-cli/references/agent-workflows.md
      provides: "Mirror of the docs/AGENT-WORKFLOWS.md Python-evaluate + verb-selection sections (this skill file already mirrors AGENT-WORKFLOWS.md per the phase 14 pattern)."
    - path: tests/integration/docsValidation.test.ts
      provides: "Three new grep-style assertions: (a) AGENT-WORKFLOWS.md contains `evaluate_requires_exec` AND `exec(`; (b) AGENT-WORKFLOWS.md contains `dap-cli launch` AND `dap-cli attach` AND a sentence body matching `/no .*--request.*flag/i` OR equivalent; (c) PLAYWRIGHT-INTEROP.md contains `not open, please run open first` AND `kill`."
  key_links:
    - from: docs/AGENT-WORKFLOWS.md (Python evaluate section)
      to: 16-01-PLAN behavior (auto-wrap + fallback envelope)
      via: "doc must accurately describe what 16-01 actually shipped — read 16-01-SUMMARY.md before writing"
      pattern: "evaluate_requires_exec"
    - from: /Users/roblou/.copilot/skills/dap-cli/references/agent-workflows.md
      to: docs/AGENT-WORKFLOWS.md
      via: "skill mirror — same wording for the Python-evaluate and verb-selection sections (phase 14 pattern)"
      pattern: "exec("
    - from: tests/integration/docsValidation.test.ts
      to: all three doc surfaces above
      via: "fs.readFile + .toContain() / regex assertion (one describe block per surface)"
      pattern: "docsValidation"
---

<objective>
Mirror the Phase 16-01 behavior + the analysis2.md §1 / §3 doc gaps into every agent-facing surface so an LLM picking up this CLI cold doesn't re-trip the same traps.

Three docs/skill targets, all enforced by `docsValidation.test.ts` so they can't silently rot:
- PYEVAL-02: Python evaluate rule (matches what 16-01 shipped — read 16-01-SUMMARY.md first)
- VERB-DOC-01: launch vs attach are separate top-level commands; no `--request` flag
- PWDOC-01: playwright-cli daemon-died recovery recipe

Per CONTEXT.md "Doc-only. Same 3 surfaces" pattern, established in phase 14.

Purpose: Eliminates the documented agent gaps from analysis2.md §Python, §1, §3 across all docs surfaces simultaneously so we don't ship inconsistent guidance.

Output: 5 modified surfaces + 1 grep-gate test file. Zero code changes.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-python-evaluate-ergonomics-auto-wrap-statements-and-document/CONTEXT.md
@.planning/phases/16-python-evaluate-ergonomics-auto-wrap-statements-and-document/16-01-SUMMARY.md

@docs/AGENT-WORKFLOWS.md
@README.md
@docs/PLAYWRIGHT-INTEROP.md
@/Users/roblou/.copilot/skills/dap-cli/SKILL.md
@/Users/roblou/.copilot/skills/dap-cli/references/agent-workflows.md
@tests/integration/docsValidation.test.ts

<!--
Phase 14 established the "in-repo docs + user-level skill mirror" pattern.
The user-level skill files at /Users/roblou/.copilot/skills/dap-cli/ are
WRITABLE in this workspace — no special permission needed.
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: In-repo docs sweep (PYEVAL-02 + VERB-DOC-01 + PWDOC-01 in-repo half)</name>
  <files>docs/AGENT-WORKFLOWS.md, README.md, docs/PLAYWRIGHT-INTEROP.md</files>
  <action>
    READ FIRST: `.planning/phases/16-python-evaluate-ergonomics-auto-wrap-statements-and-document/16-01-SUMMARY.md` — your Python evaluate text must match the auto-wrap behavior 16-01 actually shipped (heuristic boundary, opt-out shape, error envelope code/data shape). If the SUMMARY conflicts with this plan's description, the SUMMARY wins (16-01 might have iterated on the heuristic during execution).

    **docs/AGENT-WORKFLOWS.md**

    Add a new `### Python (debugpy) evaluate` subsection adjacent to the existing evaluate guidance (around L160-180 — see existing `dap-cli evaluate --expression "value + 1"` examples). Cover:
      - debugpy treats `evaluate` as an EXPRESSION; statements (import, assignment, multi-line) would normally raise SyntaxError.
      - As of phase 16, `dap-cli` detects statement-shaped Python on debugpy sessions and auto-wraps with `exec("...")` before forwarding. Pure expressions are unchanged.
      - Worked examples: `dap-cli evaluate --expression 'import os'`, `dap-cli evaluate --expression 'x = 1; x + 1'`, `dap-cli evaluate --expression '1 + 1'`.
      - Opt-out (request-args level): pass `context: 'no-auto-wrap'` (see 16-01-SUMMARY for whether a CLI flag was added).
      - Fallback: when the heuristic misses, the error envelope upgrades to `code: 'evaluate_requires_exec'` carrying `data.exec_form` — agents should read that field and retry verbatim.

    Add a new `### Choosing launch vs attach (no --request flag)` subsection (anywhere in the file's top-level "running a session" area). Cover:
      - `dap-cli launch` and `dap-cli attach` are two separate top-level commands and select the DAP `request:` field.
      - There is no `--request` flag; do not search for one.
      - When you pass `--config` with a JSON containing a `request:` field that disagrees with the verb, Phase 10 auto-routes to the correct request-side. The verb on the CLI is the source of truth.

    **README.md**

    In the existing dap-cli quick-reference / "Common commands" section (locate via grep for an `evaluate` example), add a one-paragraph callout: "On debugpy sessions, `evaluate` auto-wraps statement-shaped Python in `exec(...)` (Phase 16). When the heuristic misses, the error envelope returns `code: evaluate_requires_exec` with the exact `data.exec_form` to retry. See [docs/AGENT-WORKFLOWS.md](docs/AGENT-WORKFLOWS.md#python-debugpy-evaluate)." Anchor name MUST match the heading slug you wrote in AGENT-WORKFLOWS.md.

    **docs/PLAYWRIGHT-INTEROP.md**

    Add a new `### Recovering from a dead playwright-cli daemon` subsection. Cover:
      - Symptom: every `playwright-cli` invocation returns `not open, please run open first` even immediately after a successful `playwright-cli open`. The daemon process has died but the CLI client still thinks it can reattach.
      - Recovery: kill any lingering `playwright-cli` daemon process (`pkill -f playwright-cli`), then re-issue `playwright-cli open <url>` from scratch. The next command should work.
      - Note: this is a playwright-cli bug, not a dap-cli interop issue — fixing playwright-cli itself is out of scope (per CONTEXT.md).
  </action>
  <verify>
    <automated>grep -n "evaluate_requires_exec" docs/AGENT-WORKFLOWS.md README.md && grep -n "exec(" docs/AGENT-WORKFLOWS.md && grep -niE "no .{0,10}--request.{0,10}flag|--request flag" docs/AGENT-WORKFLOWS.md && grep -n "not open, please run open first" docs/PLAYWRIGHT-INTEROP.md && grep -niE "pkill|kill" docs/PLAYWRIGHT-INTEROP.md</automated>
  </verify>
  <done>All three grep gates above return at least one match per file; new subsections render correctly in markdown (headings nested under existing structure, examples in fenced code blocks); README anchor link target exists in AGENT-WORKFLOWS.md.</done>
</task>

<task type="auto">
  <name>Task 2: User-level skill mirror + docsValidation enforcement</name>
  <files>/Users/roblou/.copilot/skills/dap-cli/SKILL.md, /Users/roblou/.copilot/skills/dap-cli/references/agent-workflows.md, tests/integration/docsValidation.test.ts</files>
  <action>
    **/Users/roblou/.copilot/skills/dap-cli/references/agent-workflows.md**

    Mirror the two new subsections from Task 1 (`### Python (debugpy) evaluate` and `### Choosing launch vs attach`) into this file using the SAME wording. The phase 14 pattern is "skill file is a curated subset/copy of docs/AGENT-WORKFLOWS.md" — match its existing tone and section ordering. Do NOT mirror the playwright daemon-died note (PWDOC-01 is in-repo only — agents who use playwright via this skill are reading docs/PLAYWRIGHT-INTEROP.md anyway).

    **/Users/roblou/.copilot/skills/dap-cli/SKILL.md**

    The current `## dap-cli Notes` section has a `BAD/GOOD` example demonstrating manual `exec("...")` wrapping. Keep that example as-is for historical reference, but ADD one new line directly under the "GOOD" example: `Phase 16+: dap-cli evaluate auto-wraps detected statements in exec(...) on debugpy sessions; the manual wrap above is now the fallback when the heuristic misses (look for code: evaluate_requires_exec).` This preserves the agent's prior memory of the trick while pointing them at the new ergonomics.

    **tests/integration/docsValidation.test.ts**

    Read the existing file to understand its describe-block structure (it already validates other docs surfaces — match its pattern for fs.readFile + assertion). Add a new describe block `Phase 16 docs (PYEVAL-02 / VERB-DOC-01 / PWDOC-01)` containing three `it` blocks:

    1. `AGENT-WORKFLOWS.md documents Python evaluate auto-wrap`:
       - reads `docs/AGENT-WORKFLOWS.md`
       - asserts content contains `evaluate_requires_exec` AND contains `exec(` AND contains `debugpy`

    2. `AGENT-WORKFLOWS.md documents launch-vs-attach verb selection`:
       - reads `docs/AGENT-WORKFLOWS.md`
       - asserts content contains `dap-cli launch` AND `dap-cli attach`
       - asserts content matches `/no .{0,15}--request.{0,15}flag/i` (case-insensitive, allowing for short connecting words like "such", "called", "named")

    3. `PLAYWRIGHT-INTEROP.md documents daemon-died recovery`:
       - reads `docs/PLAYWRIGHT-INTEROP.md`
       - asserts content contains `not open, please run open first`
       - asserts content matches `/pkill|kill .*playwright|killing/i`

    Do NOT add assertions for the user-level skill files — those are outside the repo and the test runs in-repo. The skill mirror is enforced by the same human update-pass that touches AGENT-WORKFLOWS.md (i.e. this plan's reviewer / executor).
  </action>
  <verify>
    <automated>npx vitest run tests/integration/docsValidation.test.ts</automated>
  </verify>
  <done>All three new docsValidation tests pass green; user-level skill files contain the mirrored Python-evaluate and launch-vs-attach sections (visual diff inspect — no automated check); SKILL.md `## dap-cli Notes` retains BAD/GOOD example AND the new "Phase 16+" line.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo docs → user-level skill | The user-level skill files at `~/.copilot/skills/dap-cli/` are outside the repo's CI gate. Drift between in-repo docs and the skill is invisible to the test suite. |
| docsValidation.test.ts grep gates → real prose | Grep gates assert keyword presence, not semantic correctness. A future edit could keep the keyword while saying the wrong thing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Information Disclosure (wrong info) | Doc surface drifting from 16-01's actual behavior | mitigate | Task 1 mandates reading 16-01-SUMMARY.md first; if 16-01's heuristic boundary changed during execution, the doc must reflect the SUMMARY not this PLAN. Grep gates in docsValidation pin the keywords; a doc that loses the keyword fails CI. |
| T-16-06 | Tampering (skill drift) | User-level skill files drift from in-repo docs | accept | Out-of-repo files cannot be enforced by CI. The phase 14 pattern accepts this — the skill is a periodic mirror, not a real-time copy. Mitigation: Task 2 explicitly mirrors both new sections; future phase audits should re-check. |
| T-16-07 | Repudiation (silent removal) | A future PR removes the Python-evaluate doc section without updating the test | mitigate | docsValidation grep gates fail CI when keywords disappear. Three independent assertions cover three distinct concerns. |

</threat_model>

<verification>
- `npx vitest run tests/integration/docsValidation.test.ts` → all green (existing tests + 3 new ones).
- Manual visual check of the two user-level skill files (`SKILL.md` + `references/agent-workflows.md`) — Python-evaluate and launch-vs-attach sections present and match in-repo wording.
- `git diff` on the 5 modified files surfaces no whitespace-only changes / unrelated edits.
</verification>

<success_criteria>
- PYEVAL-02 closed: 5 surfaces (3 in-repo + 2 user-skill) describe the auto-wrap rule and the fallback envelope.
- VERB-DOC-01 closed: AGENT-WORKFLOWS.md and the user-skill mirror name `launch` vs `attach` as separate verbs and explicitly say "no --request flag".
- PWDOC-01 closed: PLAYWRIGHT-INTEROP.md contains the daemon-died symptom + recovery recipe.
- docsValidation.test.ts has 3 new assertions enforcing all three above against in-repo surfaces.
- Full vitest suite remains ≥391 passing (this plan only adds tests, never removes).
</success_criteria>

<output>
After completion, write `.planning/phases/16-python-evaluate-ergonomics-auto-wrap-statements-and-document/16-02-SUMMARY.md` per `@.github/get-shit-done/templates/summary.md`. Include:
- Diff snippets (or just the new section headings) for each of the 5 modified surfaces.
- The exact regex / `.toContain()` strings used in the new docsValidation assertions.
- Confirmation that 16-01's actual heuristic / opt-out shape (read from 16-01-SUMMARY.md) is what got documented, and a note if it differed from this plan's description.
</output>
