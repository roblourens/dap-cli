---
phase: 21-integrate-c-net-debugging-through-netcoredbg-as-a-built-in-r
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - src/adapters/registry.ts
  - src/config/launchConfig.ts
  - tests/integration/launchInference.test.ts
  - tests/integration/launchAttachAutoRoute.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Adapters listing includes netcoredbg as a built-in"
    - "VS Code type coreclr resolves to netcoredbg"
    - "VS Code type clr remains unknown unless proven supported"
    - ".dll program inference is intentionally deferred until after real DLL launch proof"
  artifacts:
    - path: "src/adapters/registry.ts"
      provides: "NetCoreDbg registry entry"
    - path: "src/config/launchConfig.ts"
      provides: "coreclr launch-config mapping and unsupported clr behavior"
  key_links:
    - from: "launchConfigTypeMap.coreclr"
      to: "netcoredbg"
      via: "resolveAdapterIdFromType"
---

<objective>
Wire NetCoreDbg into dap-cli selection surfaces without adding C# semantics to protocol core.

Purpose: satisfy locked decisions for adapter id `netcoredbg`, VS Code `coreclr`, unsupported `${command:*}`/`${input:*}`, no `.csproj` auto-build, and no `clr` mapping. `.dll` inference is explicitly deferred to Plan 21-03 after real DLL launch proof.

Output: registry entry, config/inference mappings, launch/inference regression tests.
</objective>

<scope>
Files modified:
- `src/adapters/registry.ts`
- `src/config/launchConfig.ts`
- `tests/integration/launchInference.test.ts`
- `tests/integration/launchAttachAutoRoute.test.ts`
</scope>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| `.vscode/launch.json` → dap-cli launch config | Workspace config is untrusted input |
| CLI flags/program path → adapter selection | User-controlled strings select runtime behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-21-06 | Elevation | `launchConfig.ts` | mitigate | Continue rejecting `${command:*}` and `${input:*}` variables; never execute `preLaunchTask` or `postDebugTask`. |
| T-21-07 | Tampering | `.csproj` / `.dll` inference | mitigate | Do not add `.csproj` inference or auto-build. Do not add `.dll` inference in this plan; it is gated on Plan 21-03 real DLL launch proof. |
| T-21-08 | Spoofing | `clr` type | mitigate | Do not map `clr`; assert existing `unknown_launch_type` behavior. |
| T-21-09 | Repudiation | registry listing | mitigate | Add deterministic built-in listing entry for `netcoredbg`. |
| T-21-10 | Tampering | vsdbg | mitigate | No `vsdbg` mapping or fallback. |
</threat_model>

<tasks>
<task type="auto" tdd="true">
  <name>Task 21-02-01: Register NetCoreDbg and map coreclr only</name>
  <read_first>
    - `src/adapters/registry.ts`
    - `src/config/launchConfig.ts`
    - `tests/integration/launchAttachAutoRoute.test.ts`
    - `.planning/phases/21-integrate-c-net-debugging-through-netcoredbg-as-a-built-in-r/21-CONTEXT.md`
    - `.planning/phases/21-integrate-c-net-debugging-through-netcoredbg-as-a-built-in-r/21-PATTERNS.md`
  </read_first>
  <files>src/adapters/registry.ts, src/config/launchConfig.ts, tests/integration/launchAttachAutoRoute.test.ts</files>
  <action>
    Import `createNetCoreDbgDescriptor` from `./builtins/netCoreDbg.js` and add a built-in registry entry id `netcoredbg`, label `C#/.NET Debug Adapter (NetCoreDbg)`, create callback `() => createNetCoreDbgDescriptor()`. Add `coreclr: 'netcoredbg'` to `launchConfigTypeMap`. Do not add `clr`. Add/extend integration tests proving a `type: "coreclr"` launch config routes to adapter `netcoredbg`, while `type: "clr"` returns error code `unknown_launch_type`.
  </action>
  <verify>
    <automated>npx vitest run tests/integration/launchAttachAutoRoute.test.ts tests/integration/launchInference.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "createNetCoreDbgDescriptor" src/adapters/registry.ts` finds the import/use.
    - `grep -R "coreclr: 'netcoredbg'" src/config/launchConfig.ts` finds the mapping.
    - `grep -R "clr:" src/config/launchConfig.ts` returns no `clr` mapping line.
    - Tests prove `unknown_launch_type` for `clr`.
  </acceptance_criteria>
  <done>`netcoredbg` appears as built-in, `coreclr` maps, and `clr` remains unsupported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 21-02-02: Protect unsafe launch config behavior before inference changes</name>
  <read_first>
    - `src/config/launchConfig.ts`
    - `tests/integration/launchInference.test.ts`
    - `tests/integration/launchAttachAutoRoute.test.ts`
    - `.planning/phases/21-integrate-c-net-debugging-through-netcoredbg-as-a-built-in-r/21-RESEARCH.md`
  </read_first>
  <files>tests/integration/launchInference.test.ts, tests/integration/launchAttachAutoRoute.test.ts</files>
  <action>
    Add tests that `.csproj` still produces `adapter_inference_failed` without adapter/type and that launch configs containing `${command:*}` or `${input:*}` still fail with `unsupported_launch_variable`. Add a test or explicit assertion that `type: "clr"` remains `unknown_launch_type`. Do not edit `src/config/programInference.ts` in this plan. Do not add `.dll` inference here; Plan 21-03 owns that after `netCoreDbgAdapter.test.ts` proves real built DLL launch.
  </action>
  <verify>
    <automated>npx vitest run tests/integration/launchInference.test.ts tests/integration/launchAttachAutoRoute.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -R "adapter_inference_failed" tests/integration/launchInference.test.ts` finds `.csproj` negative coverage.
    - `grep -R "unsupported_launch_variable" tests/integration/launchAttachAutoRoute.test.ts tests/integration/launchInference.test.ts` finds command/input variable coverage.
    - `grep -R "'.dll'.*netcoredbg.*coreclr" src/config/programInference.ts` returns no matches before Plan 21-03.
    - Focused launch inference tests pass.
  </acceptance_criteria>
  <done>Unsafe launch config and `.csproj` paths remain rejected; `.dll` inference remains deferred until real DLL launch proof.</done>
</task>
</tasks>

<verification>
Run:
- `npx vitest run tests/integration/launchInference.test.ts tests/integration/launchAttachAutoRoute.test.ts`
- `npx tsc --noEmit`
</verification>

<must_haves>
- Adapter registry lists `netcoredbg`.
- `coreclr` maps to `netcoredbg`.
- `clr` returns `unknown_launch_type`.
- `.dll` inference is not added until Plan 21-03 proves real DLL launch.
- `.csproj`, `${command:*}`, and `${input:*}` remain unsupported.
</must_haves>

<success_criteria>
Plan succeeds when launch/inference tests prove all mappings and non-mappings exactly.
</success_criteria>
