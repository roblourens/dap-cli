---
phase: 22-onboard-rust-debugging-through-codelldb-as-a-built-in-adapte
plan: 04
type: execute
wave: 3
depends_on: [22-01, 22-02]
files_modified:
  - src/cli/commands/setupAdapters.ts
  - scripts/dev/regen-checksums.ts
  - tests/cli/setupAdaptersCommand.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Users can explicitly prewarm or report the approved `codelldb` adapter through the existing `setup-adapters` command."
    - "Setup reports CodeLLDB cached only when the validated runtime readiness entries exist, not when a partial VSIX tree is present."
    - "Maintainers can regenerate only the R-00-approved CodeLLDB digests for review; the tool does not silently mutate the checksum source of truth."
  artifacts:
    - path: "src/cli/commands/setupAdapters.ts"
      provides: "CodeLLDB setup selection and consolidated reporting"
    - path: "scripts/dev/regen-checksums.ts"
      provides: "Reviewable CodeLLDB digest regeneration input"
  key_links:
    - from: "src/cli/commands/setupAdapters.ts"
      to: "src/adapters/provision/index.ts"
      via: "AdapterId target dispatch and cache status"
      pattern: "codelldb"
---

<objective>
Surface CodeLLDB through the established eager-prewarm/setup reporting path and maintainer checksum workflow after its provisioning gate passes.

Purpose: Lazy first use is not enough for CI, containers, or users diagnosing a native adapter cache; setup reporting must truthfully represent the full CodeLLDB runtime readiness contract.
Output: Setup-adapters selection/status integration, checksum regeneration entry, and CLI tests.
</objective>

<execution_context>@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md</execution_context>

<context>
@.planning/phases/22-onboard-rust-debugging-through-codelldb-as-a-built-in-adapte/22-GATE-RESULTS.md
@.planning/phases/22-onboard-rust-debugging-through-codelldb-as-a-built-in-adapte/22-02-SUMMARY.md
@.planning/phases/21-lazy-runtime-provisioning-of-built-in-adapters-js-debug-debu/21-03-SUMMARY.md
@src/cli/commands/setupAdapters.ts
@scripts/dev/regen-checksums.ts
@tests/cli/setupAdaptersCommand.test.ts

<gate_dependency>Read `22-GATE-RESULTS.md` first and edit nothing unless both blocking gate records are pass. If blocked, stop for explicit adapter selection/scope decision.</gate_dependency>
<interfaces>`ALL_ADAPTERS`, `ADAPTER_VERSIONS`, `expectedEntrypoint`, `isAdapterId`, `runSetupAdaptersAction`, and Commander `.choices(...)` are all closed lists that must receive `codelldb`; CodeLLDB cache readiness must match Plan 22-02's complete required tree.</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add CodeLLDB to eager setup selection, consolidated consent, and truthful status reporting</name>
  <files>src/cli/commands/setupAdapters.ts, tests/cli/setupAdaptersCommand.test.ts</files>
  <read_first>
    - .planning/phases/22-onboard-rust-debugging-through-codelldb-as-a-built-in-adapte/22-GATE-RESULTS.md
    - src/cli/commands/setupAdapters.ts
    - src/adapters/provision/codelldb.ts
    - src/adapters/provision/checksums.ts
    - tests/cli/setupAdaptersCommand.test.ts
  </read_first>
  <behavior>
    - `setup-adapters --adapter codelldb --yes` provisions only CodeLLDB and reports its approved v1.12.2 cache path/status.
    - A consolidated all-adapters setup includes `codelldb`; refusal/non-TTY still produces established consent behavior without download.
    - A missing required nested CodeLLDB runtime path is reported pending/installed-or-failed rather than incorrectly `cached`.
  </behavior>
  <action>Extend the existing setup command closed lists and option choices for `codelldb`, importing `CODELLDB_VERSION`. Make its cache-status check use the same required readiness entries/entrypoint contract established in `provisionCodeLldb`; do not classify an adapter-only partial tree as warm cache. Add tests with isolated `DAP_CLI_ADAPTERS_DIR` and synthetic VSIX/fake server for single selection, warm complete cache, partial-tree repair/status, and consolidated prompt/partial-failure reporting.</action>
  <acceptance_criteria>
    - `codelldb` is available through `--adapter` and normal all-target setup without new setup flags.
    - Tests prove complete-tree reporting and no download on a genuinely warm cache.
  </acceptance_criteria>
  <verify><automated>npx vitest run tests/cli/setupAdaptersCommand.test.ts tests/adapters/provision/codelldb.test.ts</automated></verify>
  <done>The supported prewarm/status UX includes CodeLLDB and cannot mask an incomplete native runtime installation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the human-reviewed checksum regeneration workflow for approved CodeLLDB assets</name>
  <files>scripts/dev/regen-checksums.ts</files>
  <read_first>
    - .planning/phases/22-onboard-rust-debugging-through-codelldb-as-a-built-in-adapte/22-GATE-RESULTS.md
    - scripts/dev/regen-checksums.ts
    - src/adapters/provision/checksums.ts
  </read_first>
  <behavior>
    - The maintainer script imports the CodeLLDB pinned version and emits assignments for exactly the approved platform assets with official VSIX URL shapes.
    - Running the script prints digests for human comparison/paste; it never edits checksum source files itself.
  </behavior>
  <action>Add CodeLLDB asset descriptors to `buildAssets()` using only asset names/platforms accepted by R-00. Keep the script review-oriented and avoid introducing automatic rewrite behavior. Document its output key shape to correspond directly to `CODELLDB_CHECKSUMS[CODELLDB_VERSION]`.</action>
  <acceptance_criteria>
    - The generated CodeLLDB asset set exactly equals the passed support matrix.
    - Digest output remains manual-review input, preserving Phase 21 checksum review discipline.
  </acceptance_criteria>
  <verify><automated>npm run typecheck -- --pretty false &amp;&amp; npx vitest run tests/cli/setupAdaptersCommand.test.ts</automated></verify>
  <done>Maintainers have a deterministic review tool for future approved CodeLLDB checksum updates without broadening supported assets.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Setup command to cache/provisioner | User consent can trigger native payload downloads and reporting. |
| Checksum regeneration network input to reviewed source constants | A maintainer tool observes upstream content before manual acceptance. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-04-01 | Repudiation | `setup-adapters` consent/status | mitigate | Preserve consolidated consent and typed result tests; include CodeLLDB only through existing `AdapterId` flow. |
| T-22-04-02 | Tampering | incomplete runtime reported cached | mitigate | Status readiness checks the gate-derived full runtime entry set; partial-tree test prevents false warm cache. |
| T-22-04-03 | Tampering | maintainer checksum script | mitigate | Script emits only R-00-approved VSIX assets and remains print-only for human review. |
| T-22-04-SC | Tampering | official asset identity | mitigate | Gate dependency and digest/table comparison prevent unapproved platform additions. |
</threat_model>

<verification>
- Require gate pass before edits.
- Exercise CodeLLDB single/prewarm/cache/partial-status paths with isolated cache and no public network.
- Compare regeneration entries to R-00 supported platform rows.
</verification>

<success_criteria>
- `setup-adapters` makes CodeLLDB install/report behavior discoverable without weakening cache or consent correctness.
- Checksum maintenance stays explicit, pinned, and reviewable.
</success_criteria>

<output>Create `.planning/phases/22-onboard-rust-debugging-through-codelldb-as-a-built-in-adapte/22-04-SUMMARY.md` when done.</output>
