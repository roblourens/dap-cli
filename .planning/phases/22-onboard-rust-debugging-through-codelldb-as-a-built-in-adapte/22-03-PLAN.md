---
phase: 22-onboard-rust-debugging-through-codelldb-as-a-built-in-adapte
plan: 03
type: execute
wave: 3
depends_on: [22-01, 22-02]
files_modified:
  - src/adapters/builtins/codelldb.ts
  - src/adapters/registry.ts
  - tests/adapters/codelldb.test.ts
  - tests/adapters/registry.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Users can select a lazy built-in adapter named `codelldb` for Rust-oriented CodeLLDB sessions."
    - "The descriptor invokes only the exact released-artifact standalone server shape proved in R-01 and connects to loopback."
    - "Registry listing is lazy and cannot trigger a CodeLLDB download simply by enumerating adapters."
  artifacts:
    - path: "src/adapters/builtins/codelldb.ts"
      provides: "Gate-derived CodeLLDB server descriptor factory"
      exports: ["createCodeLldbDescriptor"]
    - path: "src/adapters/registry.ts"
      provides: "Built-in `codelldb` registration"
  key_links:
    - from: "src/adapters/registry.ts"
      to: "src/adapters/builtins/codelldb.ts"
      via: "lazy descriptor factory"
      pattern: "createCodeLldbDescriptor"
    - from: "src/adapters/builtins/codelldb.ts"
      to: "src/adapters/provision/index.ts"
      via: "lazy cache/provision fallback"
      pattern: "provisionAdapter.*codelldb"
---

<objective>
Expose CodeLLDB as a built-in Rust adapter through a gate-proven loopback-only server descriptor and the existing lazy registry boundary.

Purpose: The registry and adapter-process layer must contain no guessed native invocation or network exposure behavior.
Output: `createCodeLldbDescriptor`, lazy registry entry, and descriptor/registry unit coverage.
</objective>

<execution_context>@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md</execution_context>

<context>
@.planning/phases/22-onboard-rust-debugging-through-codelldb-as-a-built-in-adapte/22-GATE-RESULTS.md
@.planning/phases/22-onboard-rust-debugging-through-codelldb-as-a-built-in-adapte/22-PATTERNS.md
@.planning/phases/22-onboard-rust-debugging-through-codelldb-as-a-built-in-adapte/22-02-SUMMARY.md
@src/adapters/builtins/delve.ts
@src/adapters/registry.ts
@tests/adapters/delve.test.ts
@tests/adapters/registry.test.ts

<gate_dependency>Before editing, require `R-00 result: pass` and `R-01 result: pass` in `22-GATE-RESULTS.md`; otherwise stop without product changes and request the explicit selection/scope decision required by the PRD.</gate_dependency>
<interfaces>`BuiltInAdapterFactory.create()` accepts an async descriptor factory; Delve demonstrates `{ kind: 'server', command, args, host: '127.0.0.1' }` and lazy provisioning, but CodeLLDB command/args/environment must be copied from R-01 evidence only.</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement the released-artifact-derived CodeLLDB server descriptor</name>
  <files>src/adapters/builtins/codelldb.ts, tests/adapters/codelldb.test.ts</files>
  <read_first>
    - .planning/phases/22-onboard-rust-debugging-through-codelldb-as-a-built-in-adapte/22-GATE-RESULTS.md
    - src/adapters/builtins/delve.ts
    - src/adapters/provision/index.ts
    - src/adapters/socketAdapter.ts
    - tests/adapters/delve.test.ts
  </read_first>
  <behavior>
    - `createCodeLldbDescriptor(optionalPath)` returns id `codelldb`, a Rust/CodeLLDB label, and a server transport whose command, args, cwd/env requirements and readiness entrypoint reproduce R-01.
    - Transport host is `127.0.0.1`; its invocation has no wildcard/remote listener option.
    - Default resolution uses the canonical validated cached CodeLLDB entrypoint or `provisionAdapter('codelldb', ...)` under existing consent behavior; it does not silently substitute host LLDB or LLVM `lldb-dap`.
  </behavior>
  <action>Create a descriptor factory following the established async lazy-built-in shape but transcribe the CodeLLDB server invocation and any runtime-relative environment/cwd from the successful R-01 record. Prefer the verified cache rather than an unrelated executable on PATH unless R-01 explicitly validated a local override/readiness contract. Keep the adapter Rust-scoped in its label and diagnostic text. Add unit tests for the explicit-path descriptor and lazy-provision failure/cache seam; assert loopback host and the exact gated arguments.</action>
  <acceptance_criteria>
    - Descriptor tests make a wildcard-address or unproved command-line change fail.
    - The factory resolves only the approved CodeLLDB implementation contract and does not mention C++ or remote debug support.
  </acceptance_criteria>
  <verify><automated>npx vitest run tests/adapters/codelldb.test.ts</automated></verify>
  <done>CodeLLDB has a loopback-only descriptor whose native process contract is traceable to live gate evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register `codelldb` as a lazy built-in without enumeration side effects</name>
  <files>src/adapters/registry.ts, tests/adapters/registry.test.ts</files>
  <read_first>
    - src/adapters/registry.ts
    - src/adapters/builtins/codelldb.ts
    - tests/adapters/registry.test.ts
    - .planning/phases/22-onboard-rust-debugging-through-codelldb-as-a-built-in-adapte/22-CONTEXT.md
  </read_first>
  <behavior>
    - `new AdapterRegistry().listAll()` contains `{ id: 'codelldb', label: <Rust CodeLLDB label>, source: 'built-in' }` without resolving/downloading it.
    - `resolve('codelldb')` delegates to the descriptor factory when a session actually needs the adapter.
  </behavior>
  <action>Import `createCodeLldbDescriptor` and add a built-in factory entry adjacent to existing built-ins. Extend registry tests with the list-only lazy assertion and a provisioned/explicit resolution assertion modeled on existing adapters. Do not add generic LLDB/C++ aliases or change custom-adapter precedence.</action>
  <acceptance_criteria>
    - Listing the registry is synchronous and does not require a CodeLLDB cache or consent.
    - Resolving the adapter yields the gated descriptor behavior through the standard factory boundary.
  </acceptance_criteria>
  <verify><automated>npx vitest run tests/adapters/registry.test.ts tests/adapters/codelldb.test.ts</automated></verify>
  <done>The approved Rust adapter is discoverable as a normal lazy built-in with the existing registry precedence intact.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Built-in descriptor to native process | Product code spawns a downloaded executable and assigns its listening transport. |
| Registry lookup to lazy provisioner | Enumeration must not cause an unexpected download or execute native code. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03-01 | Information Disclosure | server listener invocation | mitigate | Descriptor copies only R-01 loopback-proved invocation; test pins `host: '127.0.0.1'` and arguments. |
| T-22-03-02 | Spoofing / Tampering | alternate local debugger executable | mitigate | Do not silently substitute `lldb-dap` or unproved PATH candidates; resolve approved cached/provisioned payload. |
| T-22-03-03 | Repudiation | adapter enumeration downloading binaries | mitigate | Registry list test asserts the lazy factory is not executed until resolution. |
| T-22-03-SC | Tampering | native artifact dependency | mitigate | Direct dependency on passed R-00/R-01 and Plan 22-02 verified cache; stop on missing gate. |
</threat_model>

<verification>
- Confirm gate records before edits.
- Run descriptor and registry unit tests; inspect that no listener argument expands exposure beyond loopback.
</verification>

<success_criteria>
- `codelldb` is a lazy built-in adapter using only a secure, proven standalone invocation.
- Registry behavior remains side-effect-free until actual adapter resolution.
</success_criteria>

<output>Create `.planning/phases/22-onboard-rust-debugging-through-codelldb-as-a-built-in-adapte/22-03-SUMMARY.md` when done.</output>
